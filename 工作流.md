# UnityCrossFire 游戏修改器开发流程总结

## 一、整体流程概览

```
需求分析 → 游戏逆向分析 → 脚本开发 → Python UI开发 → 测试验证 → 打包发布 


## 二、各阶段详细说明（融合Agent Skills）

### 阶段一：需求分析（明确"要做什么"）

#### 🎯 核心任务
1. **明确功能目标**：用大白话写下要实现的功能
   - 例如："扩大刀具攻击范围，但只修改玩家的，不影响人机"

2. **需求拆解**：将大目标拆分为具体步骤
```
   步骤1：找到刀具攻击范围的字段/方法
   步骤2：区分玩家和人机的攻击范围
   步骤3：只修改玩家的攻击范围
   步骤4：应用到所有模式
   步骤5：切换房间不崩溃
   ```

3. **确认技术要求**：
   - ✅ 全模式适配（团队、爆破、个人、生化等）
   - ✅ 区分玩家和Bot（只对自己生效）
   - ✅ 主武器、副武器、所有背包武器都生效
   - ✅ 切换房间/重新进房间不崩溃
   - ✅ 遵守游戏逻辑，不破坏游戏流程
   - ✅ 32位游戏，注意指针和内存操作

#### 🤖 Agent Skills 应用

**1. brainstorming skill** - 创意设计探索
- **何时使用**：开始任何创造性工作前
- **核心流程**：
   ```
  探索项目上下文 → 一次问一个问题 → 提出2-3种方案 → 展示设计 → 获得批准 → 编写设计文档
  ```
- **关键原则**：
  - 一次只问一个问题，不 overwhelming
  - 必须获得用户批准才能进入实施
  - 设计要有清晰边界和明确接口
  - YAGNI原则：移除不必要的功能
  - 无占位符（NO TBD, TODO）

**2. find-skills skill** - 发现技能
- **何时使用**：寻找特定功能的实现方法
- **用途**：
  - 搜索相关技能（如：逆向分析、内存修改）
  - 验证技能质量（安装数、来源信誉）
  - 安装新技能扩展能力

#### Agent工作内容
- 使用 brainstorming 帮助拆解需求步骤
- 在dump.cs，il2cpp.h，script.json，stringliteral.json，IDA MCP反编译结果中寻找执行步骤的思路
- 对比各个方案的优缺点
- 确认技术可行性
- 编写设计文档并获得用户批准

---

### 阶段二：游戏逆向分析（找到"怎么改"）

#### 2.1 基础信息收集
**工具**：Il2CppDumper
**输入**：GameAssembly.dll + global-metadata.dat
**输出**：
- `dump.cs` - 完整的类定义（类名、方法名、字段名、偏移地址）
- `script.json` - 函数地址和偏移的JSON格式
- `stringliteral.json` - 游戏中的字符串常量的JSON格式
- `IDA MCP` - 用IDA MCP反编译后的结果
- `il2cpp.h` - Il2CppDumper生成的头文件
- `AssetStudio` - 游戏中的资源文件

#### 2.2 在dump.cs中搜索目标
**搜索关键词**：
1. 类名包含关键词的类
2. 方法名包含关键词的方法
3. 字段名包含关键词的字段
4. 列出找到的类、方法、字段的RVA地址

**关键产出**：
- 目标类名和继承关系
- 关键字段的偏移（offset）
- 操作这些字段的方法

#### 2.3 使用IDA Pro MCP反编译（最关键步骤）
**目的**：从"猜代码逻辑"变成"读代码逻辑"

**操作步骤**：
1. 在script.json中找到方法的RVA地址
2. 用IDA MCP反编译该地址
3. 查看函数的真实流程
4. 确认字段偏移是否正确
5. 找到隐藏的约束条件
6. 看清调用关系

#### 🤖 Agent Skills 应用

**systematic-debugging skill** - 系统化调试
- **何时使用**：遇到分析困难、逻辑不清晰时
- **用途**：
  - 系统分析问题根源
  - 避免表面修复
  - 确保技术严谨性

#### Agent工作内容
- 连接IDA MCP查询功能实现逻辑
- 分析从进入房间到具体功能的完整流程
- 确定是哪个类、改写了哪个字段、调用哪个方法
- 区分只对玩家生效（找继承类）还是全局生效（找顶层类）
- 使用 systematic-debugging 确保分析准确

---

### 阶段三：脚本开发（实现修改功能）

#### 3.1 选择Frida修改方式
| 场景 | 使用方式 | 说明 |
|------|---------|------|
| 只看看函数何时被调、传了什么参数 | `Interceptor.attach` | 监听函数，不改行为 |
| 阻止原函数，换成自己的逻辑 | `Interceptor.replace` | 完全替换函数 |
| 主动调游戏函数 | `NativeFunction` | 主动调用函数 |



#### 3.3 开发注意事项
- **限制日志输出**：避免日志刷屏影响性能
- **检查指针有效性**：避免空指针崩溃
- **使用 RVA 地址**：RVA地址在dump.cs中找到,RVA需要和IDA MCP反编译结果中的地址一致，不过IDA MCP的基址是100000000。
- **不要自己转换单位**：使用 `int_convert` 工具

#### 3.4 Frida脚本启动方式

**启动方式选择**：

| 场景 | 启动方式 | 说明 |
|------|---------|------|
| **有Python UI界面** | 通过UI启动 | UI会自动注入Frida脚本 |
| **无UI的调试脚本** | 通过进程名启动 | 使用 `-n` 参数指定进程名 |

**启动命令示例**：

```bash
# 方式1（推荐）：通过进程名启动
frida -n UnityCrossFire.exe -l 脚本名称.js


# 方式2：Python代码启动
import frida
session = frida.attach("UnityCrossFire.exe")
with open("脚本名称.js", "r", encoding="utf-8") as f:
    script = session.create_script(f.read())
script.load()
```

**注意事项**：
- ✅ **优先使用进程名**：不需要手动查找PID，更方便
- ✅ **游戏进程名**：UnityCrossFire.exe
- ✅ **注入时机**：游戏完全启动后再注入（进入游戏房间后）
- ⚠️ **避免重复注入**：已注入的脚本需要重启游戏才能清除


#### 🤖 Agent Skills 应用

**1. writing-plans skill** - 编写实施计划
- **何时使用**：有设计文档后，开始编码前
- **核心思路**：将设计转化为详细的、可执行的任务清单
- **特点**：
  - 每个步骤2-5分钟可完成
  - 包含完整代码、精确命令、预期输出
  - 无占位符（NO TBD, TODO）
  - TDD、DRY、YAGNI原则
- **任务结构**：
  ```
  写失败测试 → 运行确认失败 → 写最小实现 → 运行确认通过 → 提交
  ```

**2. test-driven-development skill** - 测试驱动开发
- **何时使用**：实现任何功能或修复bug时
- **铁律**：没有失败测试，就不写生产代码
- **红-绿-重构循环**：
  ```
  RED（写失败测试）→ 验证失败 → GREEN（最小实现）→ 验证通过 → REFACTOR（清理）→ 重复
  ```
- **关键原则**：
  - 必须看到测试失败
  - 写最小代码通过测试
  - 测试后重构，不添加新功能

**3. systematic-debugging skill** - 系统化调试
- **何时使用**：遇到bug、测试失败或异常行为时
- **用途**：
  - 先系统分析再提出修复
  - 确保技术严谨，避免表面修复

#### Agent工作内容
- **🔴 强制要求：开发前必须参考现有代码**
  - **开发基础**：`06-测试/00-模板/AAAAA-game_template.js`
    - 必须基于模板的结构开发
    - 使用模板中的基础框架（日志系统、模块查找、安全读取等）
  - **实现参考**：`04-正式发行版/全功能整合包x.x/`（最新版本，如v1.6）
    - Python代码：`game_modifier.py` 或 `game_modifier/` 模块化架构
    - JS脚本：`scripts/` 文件夹下的脚本
    - 参考其中的具体实现方法（NativeFunction、GameManager获取等）
  - **禁止自己发明新方法**：除非所有参考代码中都没有
  - **参考重点**：
    - NativeFunction的初始化和使用方式
    - GameManager的获取方法
    - 字段偏移和RVA地址的使用
    - Hook的安装和卸载方式
- 使用 writing-plans 编写详细实施计划
- 使用 TDD 方式生成JS文件（先写测试）
- 确保日志输出全面，方便调试
- 标记完成和未完成的部分
- 使用 systematic-debugging 优化代码逻辑

---

### 阶段四：测试开发与UI界面（06-测试 → 05-正式功能 → 04-正式发行版）

#### 4.1 实际开发流程

**第一步：在06-测试文件夹下开发**
```
06-测试/
├── 00-模板/                    ← 开发模板
│   ├── AAAAA-game_template.js  ← JS脚本模板（必须参考）
│   └── AAAAA-ui_template.py    ← Python UI模板（必须参考）
├── XX-测试-功能名称/            ← 新功能测试文件夹
│   ├── 功能名称.js             ← Frida脚本（Hook逻辑，参考模板）
│   ├── 功能名称.py             ← Python UI界面（参考模板）
│   └── 经验总结.md             ← 测试过程记录
```

**第二步：测试成功后转移到05-正式功能**
```
05-正式功能/
├── XX-功能名称/
│   ├── 功能名称.js             ← 从06-测试转移
│   ├── 功能名称.py             ← 从06-测试转移
│   └── 经验总结.md             ← 完整经验总结
```

**第三步：整合到04-正式发行版（全功能整合包）**
```
04-正式发行版/
└── 全功能整合包x.x/
    ├── game_modifier.py        ← 主程序（整合所有功能）
    ├── core/                   ← 基础设施层
    ├── features/               ← 功能定义层（新增功能）
    ├── scripts/                ← JS脚本层（新增脚本）
    ├── ui/                     ← 界面展示层
    │   ├── app.py              ← 主界面（新增UI界面）
    │   ├── widgets.py          ← UI组件
    │   └── settings_window.py  ← 设置窗口
    ├── data/                   ← 数据持久层
    └── resources/              ← 资源文件（音效、二维码等）
```

#### 4.1.1 UI界面开发流程（⚠️ 重要！新增功能必须开发UI界面）

**UI界面开发步骤**：

1. **分析现有UI架构**
   - 查看主界面文件：`ui/app.py`
   - 了解标签页结构：`_build_tab_view` 方法
   - 了解功能卡片创建：`_make_feature_card` 方法
   - 了解事件处理机制：`_toggle_feature`、`_update_switch` 方法

2. **创建UI界面**
   - 在 `_build_tab_view` 方法中添加新标签页
   - 创建 `_build_xxx_tab` 方法构建界面
   - 添加事件处理方法（按钮点击、开关切换等）
   - 在 `_update_switch` 方法中添加switch映射

3. **UI界面元素**
   - **功能开关**：使用 `CTkSwitch` 组件
   - **输入框**：使用 `CTkEntry` 组件
   - **按钮**：使用 `CTkButton` 组件
   - **下拉框**：使用 `CTkComboBox` 组件
   - **复选框**：使用 `CTkCheckBox` 组件
   - **滑块**：使用 `CTkSlider` 组件

4. **事件处理**
   - 连接Frida：通过 `self._frida.call_js_function` 调用JS函数
   - 日志输出：通过 `self._log` 输出日志
   - 状态更新：通过 `self._update_switch` 更新开关状态

**UI界面开发示例**（weapon_giver）：

```python
# 1. 在 _build_tab_view 中添加标签页
tab_weapon_giver = self.tab_view.add("赋予武器")
tab_weapon_giver_scroll = ctk.CTkScrollableFrame(tab_weapon_giver, ...)
self._build_weapon_giver_tab(tab_weapon_giver_scroll)

# 2. 创建 _build_weapon_giver_tab 方法
def _build_weapon_giver_tab(self, scroll):
    # 创建功能框架
    weapon_giver_frame = ctk.CTkFrame(scroll, ...)
    
    # 添加功能开关
    self.weapon_giver_switch = ctk.CTkSwitch(...)
    
    # 添加输入框
    self.weapon_id_entry = ctk.CTkEntry(...)
    
    # 添加按钮
    self.give_weapon_btn = ctk.CTkButton(..., command=self._give_weapon)

# 3. 添加事件处理方法
def _give_weapon(self):
    weapon_id = int(self.weapon_id_entry.get())
    result = self._frida.call_js_function('weapon_giver', 'giveweapon', [weapon_id, True, True])
    self._log(f"✅ 武器赋予成功！")

# 4. 在 _update_switch 中添加映射
switch_map = {
    ...
    'weapon_giver': self.weapon_giver_switch,
}
```

**UI界面开发注意事项**：
- ✅ **必须参考现有UI代码**：不要自己发明新的UI结构
- ✅ **保持UI风格一致**：颜色、字体、布局要和现有界面一致
- ✅ **线程安全**：耗时操作必须在独立线程中执行
- ✅ **错误处理**：所有操作都要有错误处理和日志输出
- ✅ **状态管理**：功能开关状态要正确更新

#### 4.1.2 UI界面决策标准（⚠️ 重要！新增功能时如何选择界面形式）

**决策问题**：新增功能时，是新增一个卡片，还是新增一个页面？

**决策标准**：

| 功能类型 | 界面形式 | 判断依据 | 示例 |
|---------|---------|---------|------|
| **简单功能** | 新增卡片 | 1. 只需要一个开关<br>2. 或者开关+一个滑块<br>3. 界面元素≤3个 | 快刀、无后座力、无限子弹、射速变快 |
| **中等功能** | 新增卡片（带多个控件） | 1. 需要开关+多个参数<br>2. 界面元素4-6个<br>3. 参数之间有逻辑关系 | 轻重力/高跳（开关+重力滑块+跳跃滑块+模式选择） |
| **复杂功能** | 新增页面 | 1. 需要多个输入框<br>2. 需要多个按钮<br>3. 需要复杂的交互逻辑<br>4. 界面元素>6个<br>5. 功能独立性强，不依赖其他功能 | 武器赋予（输入框+按钮+复选框+常用武器列表）<br>多人生化Buff选择（完整的配置界面） |

**决策流程图**：

```
新增功能
    ↓
评估界面元素数量
    ↓
┌─────────────────────────────────────────┐
│ 界面元素 ≤ 3个？                          │
│   ↓ 是                                   │
│   → 新增卡片（简单卡片）                   │
│   ↓ 否                                   │
│ 界面元素 4-6个？                          │
│   ↓ 是                                   │
│   → 新增卡片（带多个控件）                 │
│   ↓ 否                                   │
│ 功能独立性强？                            │
│   ↓ 是                                   │
│   → 新增页面                              │
│   ↓ 否                                   │
│   → 新增卡片（带多个控件）                 │
└─────────────────────────────────────────┘
```

**具体判断方法**：

1. **统计界面元素数量**
   - 开关：1个
   - 滑块：1个（滑块+数值显示）
   - 输入框：1个
   - 按钮：1个
   - 下拉框：1个
   - 复选框：1个

2. **评估功能独立性**
   - 是否依赖其他功能？
   - 是否需要独立的配置空间？
   - 是否需要复杂的交互逻辑？

3. **选择界面形式**
   - 简单功能（≤3个元素）→ 新增卡片
   - 中等功能（4-6个元素）→ 新增卡片（带多个控件）
   - 复杂功能（>6个元素 或 独立性强）→ 新增页面

**现有界面分类**：

**卡片形式（简单功能）**：
- 快刀：开关 + 滑块（2个元素）
- 无后座力：开关（1个元素）
- 无限子弹：开关（1个元素）
- 快速换弹：开关（1个元素）
- 自瞄：开关（1个元素）
- 射速变快：开关（1个元素）
- 滑板鞋：开关 + 滑块（2个元素）
- 无限时间：开关（1个元素）
- 金刚不坏：开关（1个元素）
- 技能无冷却：开关（1个元素）

**卡片形式（中等功能）**：
- 剑气化丝：开关 + 滑块（2个元素，但占2列）
- 轻重力/高跳：开关 + 重力滑块 + 跳跃滑块 + 模式选择（4个元素）

**页面形式（复杂功能）**：
- 多人生化Buff选择：完整的配置界面（幽灵方特性 + 人类方特性 + 应用按钮 + 连接按钮）
- 武器赋予：输入框 + 按钮 + 复选框 + 常用武器列表（多个元素）
- 聚怪：开关 + 按钮（虽然是卡片，但有特殊的按钮布局）
- 回合跳过：按钮 + 计数显示（虽然是卡片，但有特殊的按钮布局）

**特殊情况处理**：

1. **功能有特殊按钮**（如聚怪、回合跳过）
   - 虽然界面元素不多，但需要特殊的按钮布局
   - 可以用卡片形式，但需要自定义布局

2. **功能需要实时显示状态**（如回合跳过）
   - 需要显示计数或状态信息
   - 可以用卡片形式，但需要添加状态显示

3. **功能需要复杂的配置**（如多人生化Buff选择）
   - 需要多个配置项和选择框
   - 必须用页面形式

**决策建议**：

- **优先选择卡片形式**：卡片更简洁，用户体验更好
- **只有在必要时才选择页面形式**：功能确实复杂，无法用卡片表达
- **参考现有界面**：看看类似功能是怎么实现的
- **保持一致性**：同类功能应该用相同的界面形式

#### 4.1.3 功能注册（⚠️ 关键！新增功能必须注册）

**功能注册的作用**：
- 将新功能注册到 `FeatureRegistry`
- 触发 `@register_feature` 装饰器执行
- 使 `FridaManager` 能够加载对应的 JS 脚本

**功能注册步骤**：

1. **创建功能定义文件**（`features/xxx.py`）
   - 继承 `FeatureBase`
   - 使用 `@register_feature` 装饰器
   - 定义 `feature_id`、`js_filename`、`name` 等属性

2. **在 `features/__init__.py` 中导入**（⚠️ 必须做！）
   ```python
   from features.xxx import XxxFeature
   ```

3. **验证注册成功**
   - 启动程序后，功能应出现在 UI 中
   - 不应报"未知功能"错误

**常见错误**：
- ❌ 只创建了 `features/xxx.py`，忘记在 `__init__.py` 导入
- ❌ 导入语句拼写错误
- ❌ 装饰器 `@register_feature` 遗漏

**功能注册检查清单**：
- ✅ `features/xxx.py` 文件存在
- ✅ `features/__init__.py` 中有导入语句
- ✅ `@register_feature` 装饰器存在
- ✅ `feature_id` 与 JS 脚本中的模块名一致

#### 4.1.4 事件处理闭环（⚠️ 关键！JS→Python 事件必须处理）

**事件处理的作用**：
- 接收 JS 脚本通过 `send()` 发送的消息
- 在 Python 端执行相应逻辑（如调用 RPC、更新 UI、记录日志）
- 形成完整的功能闭环

**事件处理步骤**：

1. **JS 端发送事件**
   ```javascript
   send({ type: 'eventName', data: {...} });
   ```

2. **Python 端接收事件**（在 `frida_manager.py` 的 `_on_message` 方法中）
   ```python
   elif msg_type == 'eventName':
       data = payload.get('data', {})
       self._event_bus.emit('event_name', **data)
   ```

3. **UI 端处理事件**（在 `app.py` 的 `_setup_events` 方法中订阅）
   ```python
   self._event_bus.subscribe('event_name', self._on_event_name)
   ```

**常见错误**：
- ❌ JS 发送了事件，Python 端 `_on_message` 没有处理
- ❌ Python 端处理了事件，但没有执行实际操作（只记录日志）
- ❌ 事件类型名称不匹配（JS 发 `playerRespawned`，Python 等 `player_respawned`）

**事件处理检查清单**：
- ✅ JS 端 `send()` 的事件类型
- ✅ Python 端 `_on_message` 有对应的 `elif` 分支
- ✅ Python 端执行了必要的操作（调用 RPC、更新 UI 等）
- ✅ 事件类型名称完全一致

#### 4.2 整合验证（关键！）

**整合过程容易出错的地方**：
1. **执行不到位**：Agent可能没有完整执行整合步骤
2. **偷偷修改**：会偷偷改你已经写好的数据和逻辑
3. **正确改错误**：把正确的代码改成错误的
4. **随意改动**：没有更改的功能代码也会被随意改动
5. **功能冲突**：新增功能和原有功能可能冲突

**必须验证的内容**：
- ✅ 原有功能代码是否被修改（对比差异）
- ✅ 原有数据是否被修改
- ✅ 新增功能是否正确集成
- ✅ 新增功能和原有功能是否冲突
- ✅ 整合后的代码逻辑是否正确

**测试验证分工**（AI + 用户协作）：

| 验证类型 | 执行者 | 验证内容 | 验证方法 |
|---------|--------|---------|---------|
| **代码层面验证** | AI | 语法、逻辑、编译、原有代码是否被修改 | 静态分析、代码对比、语法检查 |
| **游戏层面验证** | 用户 | 功能效果、是否闪退、全模式测试 | 手动测试、游戏环境验证 |

**JS脚本整合的正确方式**：

❌ **错误方式**：直接复制整个JS文件
```bash
# 错误示例
cp gravity_modifier-v8.js scripts/gravity.js
```

✅ **正确方式**：迁移核心逻辑（游戏修改逻辑严格迁移，其他按整合包规范调整）

**必须迁移的内容**：
- ✅ **RVA地址定义**：游戏函数地址、字段偏移
- ✅ **Hook逻辑**：Interceptor.attach、Interceptor.replace
- ✅ **NativeFunction定义**：游戏函数调用
- ✅ **游戏修改逻辑**：字段读写、状态判断

**按整合包规范调整的内容**：
- ⚠️ **日志系统**：使用整合包的日志规范
- ⚠️ **RPC接口**：按照整合包的RPC规范定义
- ⚠️ **错误处理**：统一整合包的错误处理方式

**不需要迁移的内容**：
- ❌ **调试代码**：临时测试代码、调试日志
- ❌ **独立UI逻辑**：整合包有自己的UI系统
- ❌ **不兼容部分**：与整合包架构冲突的代码

#### 4.2.1 整合验证检查清单（⚠️ 新增功能必须逐项检查）

**功能注册检查**：
- ✅ `features/xxx.py` 文件存在且语法正确
- ✅ `features/__init__.py` 中有导入语句
- ✅ `@register_feature` 装饰器存在
- ✅ `feature_id` 与 JS 模块名一致

**JS 脚本检查**：
- ✅ `scripts/xxx.js` 文件存在
- ✅ RVA 地址与 IDA 反编译结果一致
- ✅ `modules.xxx` 模块定义存在
- ✅ `enable()` 和 `disable()` 方法存在
- ✅ RPC 接口定义完整

**事件处理检查**：
- ✅ JS `send()` 的事件类型在 `_on_message` 中有处理
- ✅ Python 端执行了必要操作（不只是记录日志）
- ✅ 事件类型名称完全一致

**UI 界面检查**：
- ✅ UI 组件正确绑定事件处理方法
- ✅ `_update_switch` 中有对应的 switch 映射
- ✅ 功能开关状态正确更新

**原有功能检查**：
- ✅ 对比 git diff，确认原有代码未被意外修改
- ✅ 原有功能测试通过（快刀、无后座力等）
- ✅ 新功能与原有功能无冲突


#### 4.3 整合包架构（04-正式发行版）

**注意**：这是多个功能整合后的最终架构，放在04-正式发行版，版本会持续更新（v1.3、v1.4、v1.5、v1.6...）

```
game_modifier/
├── core/          ← 基础设施层：管连接、管事件、管配置、管快捷键、管音效
├── features/      ← 功能定义层：定义"有哪些功能"以及"怎么跟 Frida 通信"
├── scripts/       ← JS 脚本层：真正修改游戏内存的 Frida Hook 代码
├── ui/            ← 界面展示层：用户看到的窗口、按钮、开关
└── data/          ← 数据持久层：保存用户的设置和状态
```

#### 🤖 Agent Skills 应用

**1. test-driven-development skill** - 测试驱动开发
- **用途**：在06-测试文件夹下开发新功能
- **AI测试范围**：只能测试代码层面（语法、逻辑、编译）
- **用户测试范围**：游戏层面测试（玩家体验修改后的效果、功能是否正常）
- **流程**：
  - 使用模板创建测试文件
  - 编写JS脚本（Hook逻辑，参考整合包下其他同类型组件的规范）
  - 编写Python UI界面（参考整合包下其他同类型组件的规范）
  - AI验证代码层面（语法、逻辑）
  - 用户手动测试游戏层面（功能效果、是否闪退）
  - 记录测试过程和问题

**2. writing-plans skill** - 编写实施计划
- **用途**：规划功能开发步骤
- **特点**：
  - 明确RVA地址和字段偏移
  - 设计Hook策略
  - 规划UI界面布局

**3. verification-before-completion skill** - 完成前验证（最重要）
- **用途**：
  - 测试成功后转移到05-正式功能前验证
  - 整合到04-正式发行版前验证（关键！）
- **验证项**：
  - 功能在游戏中正常工作
  - 全模式测试通过
  - 无崩溃、无错误
  - 日志输出正确
  - **整合验证**：原有功能代码未被修改、新增功能正确集成、无功能冲突

**4. requesting-code-review skill** - 请求代码审查
- **用途**：整合到04-正式发行版前审查
- **验证项**：
  - 对比版本差异
  - 检查原有功能是否被修改
  - 检查新增功能是否正确
  - 检查是否有功能冲突

#### Agent工作内容
- 使用 writing-plans 规划功能开发
- 使用 TDD 方式开发测试代码
- 基于模板生成JS和Python文件
- 在06-测试文件夹下测试验证
- 使用 verification-before-completion 确认测试成功
- 转移到05-正式功能文件夹
- 编写经验总结文档
- **整合到04-正式发行版**：
  - 使用 verification-before-completion 验证整合结果
  - 使用 requesting-code-review 对比版本差异
  - 确保原有功能代码未被修改
  - 确保新增功能正确集成
  - 确保无功能冲突

---

### 阶段五：测试验证（三个阶段，偏重不同）

#### 5.1 三个测试阶段的偏重

| 测试阶段 | 主要测试内容 | 测试重点 |
|---------|------------|---------|
| **06-测试** | 功能能不能跑通 | 会不会让游戏闪退、基本功能是否正常 |
| **05-正式功能** | 一般不怎么测试 | 可以直接用了（已在06-测试验证过） |
| **04-正式发行版** | 功能整合测试 | 多个功能同时开启是否冲突、整合后是否正常 |

#### 5.2 测试清单
1. **全模式适用**：团队、个人、生化、多人生化等所有模式
2. **区分玩家和人机**：确保只对玩家生效
3. **武器兼容性**：主武器、副武器、捡来的武器、所有背包武器
4. **房间切换**：切换房间、重新进房间不崩溃
5. **多武器测试**：多试验几把枪，避免只有前面几个生效
6. **功能冲突**：检测同时开启多个功能是否冲突（04-正式发行版重点）
7. **开关状态**：功能启动就生效，关闭就失效
8. **数值范围**：速度倍数等数值是否越界（游戏内部校验会重置）
9. **性能测试**：是否卡顿、掉帧（定时器高频写入会导致严重卡顿）
10. **Hook点验证**：某些模式可能走不同的函数路径，需要验证Hook点是否覆盖所有情况
11. **RVA地址验证**（⚠️ 重要！在06-测试阶段执行）：
    - 从script.json读取RVA地址
    - 从dump.cs验证字段偏移
    - 从IDA MCP反编译结果验证地址正确性
    - 注意：IDA MCP基址是0x10000000，需要转换
    - 多源交叉验证，确保地址准确无误

**功能冲突测试分工**（AI + 用户协作）：

| 测试阶段 | 执行者 | 测试内容 | 测试方法 |
|---------|--------|---------|---------|
| **静态分析** | AI | 代码层面检验 | 分析代码结构、检查冲突点 |
| **动态测试** | 用户 | 游戏环境测试 | 同时开启多个功能、观察游戏表现 |

**AI静态分析检验项**：
- ✅ **Hook点冲突**：两个功能Hook同一个函数（可能导致逻辑覆盖）
- ✅ **变量名冲突**：全局变量重名（可能导致状态混乱）
- ✅ **资源冲突**：快捷键重复、音效文件冲突
- ✅ **依赖冲突**：不同版本的库、不兼容的模块
- ✅ **逻辑冲突**：功能逻辑相互矛盾（如一个加速、一个减速）

**用户动态测试项**：
- ✅ **同时开启测试**：同时开启多个功能，观察游戏是否正常
- ✅ **功能效果验证**：每个功能效果是否符合预期
- ✅ **稳定性测试**：长时间运行是否闪退、卡顿
- ✅ **边界情况**：极端参数、快速开关功能等

**冲突测试流程**：
```
AI静态分析 → 发现潜在冲突 → 报告给用户 → 用户动态测试 → 反馈结果 → 修复冲突
```

#### 5.3 功能测试验证流程（⚠️ 重要！在打包前必须完成）

**测试时机**：在功能开发完成后，整合到04-正式发行版之前

**测试步骤**：

**步骤一：启动测试环境**
```
1. 启动游戏：UnityCrossFire.exe
2. 运行主程序：python game_modifier/main.py
3. 确认程序正常启动，UI正常显示
```

**步骤二：连接测试**
```
1. 等待程序自动连接游戏（或手动点击连接按钮）
2. 确认连接成功（状态栏显示绿色）
3. 确认日志显示"已连接到游戏进程"
```

**步骤三：功能开关测试**
```
对每个功能进行以下测试：
1. 开启功能 → 确认功能生效
2. 关闭功能 → 确认功能失效
3. 多次开关 → 确认状态切换正常
```

**步骤四：滑块调节测试**
```
对有滑块的功能进行以下测试：
1. 拖动滑块 → 确认数值实时更新
2. 极限值测试 → 确认不会越界
3. 功能效果验证 → 确认效果符合预期
```

**步骤五：按钮功能测试**
```
对有按钮的功能进行以下测试：
1. 点击按钮 → 确认功能执行
2. 查看日志 → 确认执行结果正确
3. 游戏内验证 → 确认游戏内效果正确
```

**步骤六：快捷键测试**
```
1. 绑定快捷键 → 确认绑定成功
2. 按下快捷键 → 确认功能触发
3. 重启程序 → 确认快捷键绑定保存
```

**步骤七：音效测试**
```
1. 开启功能 → 确认音效播放
2. 关闭功能 → 确认音效播放
3. 音量调节 → 确认音量变化
```

**步骤八：设置保存和恢复测试**
```
1. 开启多个功能 → 记录状态
2. 关闭程序 → 重新启动
3. 确认功能状态自动恢复
```

**步骤九：房间切换测试**
```
1. 开启功能 → 进入游戏房间
2. 切换房间 → 确认功能仍然生效
3. 退出房间 → 重新进入 → 确认功能仍然生效
4. 确认无崩溃、无闪退
```

**测试清单**：
- ✅ 连接游戏成功
- ✅ 所有功能开关正常（快刀、无后座力、无限子弹等）
- ✅ 滑块调节正常（快刀速度、移动速度等）
- ✅ 按钮功能正常（聚怪、跳过回合）
- ✅ 快捷键绑定正常
- ✅ 音效播放正常
- ✅ 设置保存和恢复正常
- ✅ 切换房间不崩溃

**测试分工**：

| 测试类型 | 执行者 | 测试内容 |
|---------|--------|---------|
| **自动化测试** | AI | 代码静态分析、冲突检测、路径检查 |
| **实际游戏测试** | 用户 | 在游戏环境中测试所有功能 |

#### 5.4 代码清理（打包前必须完成）

**清理时机**：功能测试验证通过后，打包前

**清理步骤**：

**步骤一：删除调试代码**
```
1. 搜索所有print语句 → 删除临时调试用的print
2. 搜索所有console.log → 删除临时调试用的console.log
3. 保留必要的日志输出（sendLog、self._log等）
```

**步骤二：检查硬编码路径**
```
1. 搜索所有绝对路径 → 改为相对路径或config.py中的配置
2. 检查资源文件路径 → 确保使用config.py中的路径配置
3. 检查数据文件路径 → 确保使用config.py中的路径配置
```

**步骤三：检查资源文件路径**
```
1. 检查音效文件路径 → 确保路径正确
2. 检查图片文件路径 → 确保路径正确
3. 检查JS脚本路径 → 确保路径正确
4. 检查数据文件路径 → 确保路径正确
```

**步骤四：代码审查**
```
1. 检查是否有TODO、FIXME等占位符 → 移除或实现
2. 检查是否有注释掉的代码 → 删除无用代码
3. 检查是否有重复代码 → 合并或提取
4. 检查是否有未使用的导入 → 删除无用导入
```

**清理验证清单**：
- ✅ 无临时调试代码（print、console.log）
- ✅ 无硬编码路径（全部使用config.py）
- ✅ 资源文件路径正确
- ✅ 无TODO、FIXME占位符
- ✅ 无注释掉的代码
- ✅ 无重复代码
- ✅ 无未使用的导入

#### 5.5 常见问题与解决方案（从经验总结中收集）

**功能实现问题**：

| 问题 | 解决方案 | 参考文档 |
|------|---------|---------|
| Bot在玩家之前挥刀，会把Bot的刀错认为玩家的 | 使用Player.get_isMyPlayer()准确判断玩家身份 | [快刀经验总结.md](05-正式功能/04-快刀/快刀经验总结--与IDA的结合.md) |
| 日志总是输出硬编码，而不是get的值 | 使用动态读取的值，而不是硬编码常量 | - |
| 关闭功能仍然生效 | 确保Hook卸载正确，清除所有状态 | - |
| 快刀和攻击距离同时开启导致另一个失效 | 检查功能冲突，确保Hook点不冲突 | - |
| 只缓存前几个武器，导致切换背包后失效 | 移除缓存，使用实时判断Weapon.get_isMyWeapon() | [快刀经验总结.md](05-正式功能/04-快刀/快刀经验总结--与IDA的结合.md#L60-L80) |
| 定时器高频写入，造成严重卡顿 | 移除定时器，改为事件驱动（Hook触发时修改） | [快刀经验总结.md](05-正式功能/04-快刀/快刀经验总结--与IDA的结合.md#L82-L90) |
| 数值越界，游戏内部校验会重置 | 对写入值做范围限制，避免超过游戏最大有效值 | [快刀经验总结.md](05-正式功能/04-快刀/快刀经验总结--与IDA的结合.md#L92-L100) |
| Hook点不对导致某些模式无效 | 使用GetKnifeAttackData而不是PlayKnifeAttackAnim | [knife_range_v21_技术总结.md](05-正式功能/07-增加刀的攻击范围/knife_range_v21_技术总结.md#L100-L120) |
| Bot/玩家识别逻辑有缺陷导致某些模式不生效 | 移除Bot/玩家区分，直接对所有武器生效 | [无后座力经验总结.md](05-正式功能/02-无后座力/无后座力经验总结.md) |
| 无限弹匣功能只改变UI显示，实际无效 | 删除无效的UI Hook，实现真正的弹匣锁定 | [无限弹匣+快速换弹-经验总结.md](05-正式功能/05-快速换弹BUFF/无限弹匣+快速换弹-经验总结.md) |

**代码实现问题**：

| 问题 | 解决方案 | 参考文档 |
|------|---------|---------|
| RVA地址经常出错（从dump.cs读取但未验证） | 从script.json读取，使用IDA MCP验证 | - |
| 创建新文件时不会沿用正确实现，而是自行换了一套实现方式 | 明确指示原有代码逻辑和数据一个字都不要更改 | - |
| 日志一直在不断变化，上下文记忆不好 | 减少创建新文件，保持上下文一致性 | - |
| onLeave中调用NativeFunction导致跨线程崩溃 | 使用Interceptor.replace劫持，在游戏线程安全调用 | [knife_range_v21_技术总结.md](05-正式功能/07-增加刀的攻击范围/knife_range_v21_技术总结.md#L30-L50) |
| 初始化期间调NativeFunction导致闪退 | 不在attach的onEnter中调用任何NativeFunction | [knife_range_v21_技术总结.md](05-正式功能/07-增加刀的攻击范围/knife_range_v21_技术总结.md#L80-L100) |

**整合问题**：

| 问题 | 解决方案 | 验证方法 |
|------|---------|---------|
| 整合时偷偷修改原有功能代码 | 使用verification-before-completion验证整合结果 | 对比版本差异 |
| 把正确的代码改成错误的 | 使用requesting-code-review审查 | 检查原有功能是否被修改 |
| 没更改的功能代码也被随意改动 | 明确指示只修改新增功能部分 | 检查代码差异 |
| 新增功能和原有功能冲突 | 测试多个功能同时开启 | 功能冲突测试 |

#### 功能整合完整流程（⚠️ 重要！）

**整合到全功能整合包需要修改的文件**：

| 文件 | 修改内容 | 说明 |
|------|---------|------|
| `core/config.py` | FEATURES_INFO添加功能注册 | 否则报"未知功能"错误 |
| `core/frida_manager.py` | _build_rpc_exports添加RPC导出 | 否则Python无法调用JS函数 |
| `features/xxx.py` | 创建功能定义文件 | 定义功能元数据和JS代码加载 |
| `scripts/xxx.js` | 创建或更新JS脚本 | 实现游戏修改逻辑 |
| `ui/app.py` | 添加UI界面 | 实现用户交互 |

**步骤一：功能注册（config.py）**

在`FEATURES_INFO`字典中添加功能注册：
```python
FEATURES_INFO = {
    # ... 原有功能 ...
    'weapon_giver': {'name': '武器赋予', 'icon': '🔫', 'category': 'weapon'},
}
```

**步骤二：RPC导出（frida_manager.py）**

在`_build_rpc_exports`方法中添加RPC导出函数：
```python
def _build_rpc_exports(self):
    return r"""
rpc.exports = {
  // ... 原有导出 ...
  giveweapon: function(weaponId, autoGiveUp, autoSelect) {
    if (!modules.weapon_giver) return JSON.stringify({ ok: false, msg: '武器赋予模块未加载' });
    var result = modules.weapon_giver.giveweapon(weaponId, autoGiveUp, autoSelect);
    return JSON.stringify({ ok: result ? true : false, result: result });
  },
  setrespawnweapon: function(weaponId, weaponName) {
    if (!modules.weapon_giver) return JSON.stringify({ ok: false });
    var result = modules.weapon_giver.setrespawnweapon(weaponId, weaponName);
    return JSON.stringify({ ok: result });
  },
  clearrespawnweapon: function() {
    if (!modules.weapon_giver) return JSON.stringify({ ok: false });
    var result = modules.weapon_giver.clearrespawnweapon();
    return JSON.stringify({ ok: result });
  }
};
"""
```

**步骤三：Python调用JS的正确方式**

❌ **错误方式**：使用不存在的方法
```python
# 错误：FridaManager没有call_js_function方法
result = self._frida.call_js_function('weapon_giver', 'giveweapon', [weapon_id, True, True])
```

✅ **正确方式**：使用call_export方法
```python
# 正确：使用call_export调用RPC导出函数
result = self._frida.call_export('giveweapon', weapon_id, True, True)
if result and result.get('ok'):
    # 处理成功结果
else:
    # 处理失败：result.get('msg', '未知错误')
```

**FridaManager可用方法**：

| 方法 | 用途 | 参数 | 返回值 |
|------|------|------|--------|
| `send_toggle(feature, enable)` | 发送功能开关指令 | feature: 功能ID, enable: True/False | None |
| `call_export(name, *args)` | 调用RPC导出函数 | name: 函数名, *args: 参数 | dict（JSON解析结果） |
| `find_pid()` | 查找游戏进程PID | None | int或None |
| `connect(pid)` | 连接游戏进程 | pid: 进程ID（可选） | bool |
| `disconnect()` | 断开连接 | None | None |

**常见错误及解决方案**：

| 错误信息 | 原因 | 解决方案 |
|---------|------|---------|
| `未知功能: xxx` | 功能未在FEATURES_INFO注册 | 在config.py的FEATURES_INFO中添加功能注册 |
| `'FridaManager' object has no attribute 'call_js_function'` | 使用了不存在的方法 | 改用`call_export`方法 |
| `xxx模块未加载` | JS脚本未正确加载 | 检查scripts/xxx.js文件是否存在和正确 |
| `RPC导出函数未定义` | frida_manager.py未添加RPC导出 | 在_build_rpc_exports中添加对应函数 |

**整合验证清单**：

- [ ] config.py中FEATURES_INFO已添加功能注册
- [ ] frida_manager.py中_build_rpc_exports已添加RPC导出
- [ ] features/xxx.py功能定义文件已创建
- [ ] scripts/xxx.js脚本已创建或更新
- [ ] ui/app.py中UI界面已添加
- [ ] Python调用使用call_export而非call_js_function
- [ ] RPC导出函数返回JSON格式（包含ok字段）
- [ ] 原有功能代码未被修改

**v1.6版本更新经验**（2026-05-29）：

| 经验类型 | 经验内容 | 说明 |
|---------|---------|------|
| **功能对比分析** | 更新前先对比新旧版本功能差异 | 避免不必要的工作，有些功能可能已经包含 |
| **代码执行思路** | 在执行代码更新前，先制定详细的执行思路并获得用户确认 | 确保执行方向正确，避免返工 |
| **RVA地址验证时机** | RVA地址验证应在06-测试阶段执行 | 整合到04-正式发行版前必须验证地址正确性 |
| **主线程调度** | GiveWeapon等Unity API必须在主线程执行 | Hook ModeBase.Update，通过任务队列调度到主线程 |
| **代码迁移原则** | 只迁移核心逻辑，遵守整合包规范 | 使用整合包的日志系统、RPC接口、错误处理 |
| **原有功能保护** | 除了明确需要更新的部分，其他代码一个字都不改 | 避免引入新的bug |

#### 🤖 Agent Skills 应用

**1. verification-before-completion skill** - 完成前验证（最重要）
- **何时使用**：声称完成、修复或通过前
- **铁律**：没有新鲜验证证据，就不能声称完成
- **验证流程**：
```
  识别验证命令 → 运行完整命令 → 读取完整输出 → 确认结果 → 才能声称
  ```
- **常见验证场景**：
  - 测试通过：需要测试命令输出显示0失败
  - 构建成功：需要构建命令退出码为0
  - Bug修复：需要测试原始症状通过
- **红牌警告**：
  - 使用"应该"、"可能"、"看起来"
  - 表达满意前未验证
  - 信任agent成功报告

**2. systematic-debugging skill** - 系统化调试
- **何时使用**：测试失败、发现bug时
- **用途**：
  - 执行测试步骤并记录结果
  - 分析日志找出问题所在
  - 系统化修复，避免表面修复

#### Agent工作内容
- 使用 verification-before-completion 确保每个测试都真实通过
- 执行测试步骤并记录结果
- 使用 systematic-debugging 分析日志找出问题所在
- 优化代码修复bug
- **必须运行验证命令并确认输出**

---

### 阶段六：打包发布

#### 6.1 打包前准备

**前提条件**：
- ✅ 功能测试验证已通过（阶段五）
- ✅ 代码清理已完成（阶段五）
- ✅ 所有功能在游戏环境中测试正常

**准备工作**：
1. **检查依赖文件**：
   - requirements.txt是否存在
   - 所有依赖是否正确列出
   
2. **检查资源文件**：
   - 音效文件是否完整
   - 图片文件是否完整
   - JS脚本是否完整
   - 数据文件是否完整

#### 6.2 创建打包配置

**步骤一：创建requirements.txt**
```
在`全功能整合包x.x/`目录下创建requirements.txt，包含所有依赖：
- customtkinter>=5.2.0
- frida>=16.0.0
- frida-tools>=12.0.0
- psutil>=5.9.0
- pynput>=1.7.6
- keyboard>=0.13.5
- pygame>=2.5.0
- Pillow>=10.0.0
- pyinstaller>=6.0.0
```

**步骤二：创建PyInstaller配置**
```
创建game_modifier.spec文件，配置：
- 入口文件：game_modifier/main.py
- 资源文件：scripts/、data/、资源/
- 隐式导入：customtkinter、frida等
- 输出名称：游戏修改器控制台_vx.x.exe
```

#### 6.3 执行打包

**步骤一：安装依赖**
```bash
pip install -r requirements.txt
```

**步骤二：执行打包命令**
```bash
pyinstaller game_modifier.spec
```

**步骤三：检查打包输出**
```
- 输出目录：dist/
- 生成文件：游戏修改器控制台_vx.x.exe
- 临时文件：build/（可删除）
```

#### 6.4 打包后测试

**测试目的**：验证打包后的exe能否正常运行

**测试步骤**：
1. **启动测试**：双击exe → 确认正常启动
2. **界面测试**：确认UI正常显示
3. **资源测试**：确认音效、图片正常加载
4. **连接测试**：启动游戏 → 确认能正常连接

**注意**：此阶段只测试exe能否正常运行，不做功能测试（功能测试已在阶段五完成）

#### 6.5 压缩打包

**步骤一：检查exe文件**
```
确认dist/目录下生成了exe文件：
- 游戏修改器控制台_vx.x.exe
```

**步骤二：压缩打包**
```
使用压缩软件（如7-Zip、WinRAR）将exe压缩为zip格式：
- 输入：游戏修改器控制台_vx.x.exe
- 输出：游戏修改器控制台_vx.x.zip
```

**注意**：
- 只需要将exe压缩成zip格式即可

#### 6.6 发布后提醒

**提醒用户**：
- ⚠️ 请自行备份源代码
- ⚠️ 请自行管理Git版本
- ⚠️ 请自行更新版本记录

#### 6.7 打包注意事项（容易出错！）

**⚠️ PyInstaller路径处理（最重要！）**：
- ⚠️ **打包后路径变化**：打包后，资源文件和脚本文件会被解压到`sys._MEIPASS`临时目录
- ⚠️ **必须在config.py中区分打包和开发环境**：
  ```python
  if getattr(sys, 'frozen', False):
      # 打包后：使用sys._MEIPASS
      SCRIPTS_DIR = os.path.join(sys._MEIPASS, "game_modifier", "scripts")
      RESOURCE_DIR = os.path.join(sys._MEIPASS, "资源")
  else:
      # 开发时：使用正常路径
      SCRIPTS_DIR = os.path.join(APP_DIR, "scripts")
      RESOURCE_DIR = os.path.join(os.path.dirname(APP_DIR), "资源")
  ```
- ✅ **DATA_DIR必须放在APP_DIR**：因为data目录需要可写（保存配置）
- ✅ **SCRIPTS_DIR和RESOURCE_DIR放在sys._MEIPASS**：因为它们是只读的
- ⚠️ **常见错误**：打包后所有功能失效，通常是因为路径配置错误

**资源文件**：
- ⚠️ **经常忘记放资源文件**：音效、二维码等资源文件
- ✅ 必须检查resources文件夹是否完整打包进EXE
- ✅ 验证打包后的EXE能正常加载资源

**依赖和环境**：
- ⚠️ **忘记把所有依赖打包**：Python环境、第三方库
- ✅ 使用PyInstaller的--add-data参数添加资源
- ✅ 使用--hidden-import添加隐式导入
- ✅ 验证打包后的EXE在干净环境能正常运行

**验证清单**：
- ✅ 资源文件是否完整（音效、二维码）
- ✅ 所有依赖是否打包
- ✅ EXE能正常启动
- ✅ 所有功能正常工作
- ✅ 无崩溃、无错误

#### 🤖 Agent Skills 应用

**1. verification-before-completion skill** - 完成前验证
- **用途**：打包前验证所有功能正常
- **验证项**：
  - 所有测试通过
  - EXE能正常启动
  - 所有功能正常工作
  - 无崩溃、无错误

**2. requesting-code-review skill** - 请求代码审查
- **何时使用**：完成主要功能、准备合并前
- **用途**：
  - 验证工作满足要求
  - 发现潜在问题
  - 确保代码质量

**3. receiving-code-review skill** - 接收代码审查
- **何时使用**：收到审查反馈后
- **用途**：
  - 实施建议前进行技术验证
  - 确保技术严谨
  - 避免盲目实施有问题的建议

#### Agent工作内容
- 使用 verification-before-completion 验证打包前所有功能
- 打包Python文件成EXE
- 整合新功能到全功能包
- 使用 requesting-code-review 请求审查
- 对比版本差异，确保没有破坏原有功能

---

### 阶段七：版本管理

#### 7.1 Git管理策略
- **dev分支**：开发新功能、测试
- **main分支**：稳定版本、发布版本
- **注意**：Git和远程GitHub由用户自己处理，AI不需要管理

#### 7.2 文档管理
- 每个功能编写经验总结文档
- 记录实现方案、遇到的问题、解决方案
- 更新工作流和提示词文档

#### 🤖 Agent Skills 应用

**注意**：Git相关技能不需要使用，由用户自己管理

#### Agent工作内容
- 编写经验总结文档
- 更新工作流和提示词文档
- **不管理Git和远程GitHub**

---

## 三、核心工具与Skills分工

### 3.1 技术工具

| 工具 | 给我什么 | 什么时候用 |
|------|---------|-----------|
| **dump.cs** | 类名、字段偏移、方法签名 | 找类和字段结构 |
| **script.json** | 方法 RVA 地址 | 找函数地址 |
| **IDA Pro MCP** | 方法的真实代码逻辑 | 看函数内部在干什么 |
| **Frida** | 动态插桩、内存修改 | 实现修改功能 |
| **CustomTkinter** | Python 可视化界面 | 替代命令行，一键操作 |
| **Python+PyInstaller** | 打包成EXE | 分发给用户 |

### 3.2 Agent Skills 工具

| Skill | 核心作用 | 使用阶段 |
|-------|---------|---------|
| **brainstorming** | 需求探索、设计验证 | 阶段一 |
| **find-skills** | 发现新技能 | 阶段一 |
| **writing-plans** | 编写实施计划 | 阶段三、四 |
| **test-driven-development** | TDD开发 | 阶段三、四 |
| **systematic-debugging** | 系统化调试 | 阶段二、三、五 |
| **verification-before-completion** | 完成前验证 | 阶段四、五、六 |
| **requesting-code-review** | 请求代码审查 | 阶段四、六 |
| **receiving-code-review** | 接收代码审查 | 阶段六 |

**注意**：Git相关技能（using-git-worktrees、finishing-a-development-branch）由用户自己管理，AI不使用

---

## 四、关键技术要点

### 4.1 获取实例地址的方法
1. **Hook实例方法的this指针**（最常用）
2. **读取全局/静态变量**
3. **从其他已知对象链式读取**
4. **通过构造函数Hook**
5. **通过静态方法/属性的返回值**

### 4.2 读写字段数值的方法
1. **Hook + 实例地址 + 偏移**：直接修改字段值
2. **Hook返回值**：改写方法返回值，不需要实例地址

### 4.3 玩家识别方法
- `Weapon.get_isMyWeapon()` (RVA: 0xB6E1D0) - 判断武器是否属于玩家
- `Player.get_isMyPlayer()` (RVA: 0xB55FD0) - 判断Player实例是否是玩家
- `Entity.team` (字段 0x20) - 0=潜伏者 1=保卫者

---

## 五、版本演进历程

### v1.0 - v1.2：单功能开发阶段
- 开发各个独立功能（无限子弹、快刀、自瞄等）
- 积累经验，发现问题

### v1.3：UI界面升级
- 改进用户界面
- 添加更多功能

### v1.4：功能整合与冲突解决
- 解决Interceptor.replace冲突
- UI界面进一步升级

### v1.5：多人生化BUFF选择器
- 添加多人生化模式特性选择
- UI升级

### v1.6：模块化架构重构
- 完全重构为模块化架构
- 分层设计：core/features/scripts/ui/data
- 提高代码可维护性和扩展性

---

## 六、经验教训总结

### 6.1 开发经验
1. **不要让DeepSeek直接写代码**：错误太多，让它找思路或找错误
2. **必须用IDA MCP反编译**：不能只靠dump.cs猜测逻辑
3. **优先找基类方法**：实现全模式适配
4. **不缓存指针**：每次动态判断，避免切换房间崩溃
5. **日志要全面**：方便调试，找出问题所在

### 6.2 常见陷阱
1. **只缓存前几个武器**：导致切换背包后失效
2. **定时器高频写入**：造成严重卡顿
3. **数值越界**：游戏内部校验会重置
4. **功能冲突**：多个功能同时开启可能互相影响
5. **Bot识别错误**：需要准确区分玩家和Bot

### 6.3 Skills使用经验
1. **验证优先**：完成前必须验证，证据优于断言
2. **TDD铁律**：没有失败测试，就不写生产代码
3. **设计先行**：先设计再实施，获得批准再编码
4. **无占位符**：完整代码、精确命令、明确输出
5. **系统化调试**：先分析再修复，避免表面修复
6. **整合验证**：整合到04-正式发行版时必须验证原有功能未被修改

---

## 七、高级Skills协作模式

### 7.1 并行开发模式

**dispatching-parallel-agents skill** - 派发并行代理
- **何时使用**：面对2+独立任务时
- **条件**：无共享状态、无顺序依赖
- **用途**：提高效率，独立任务并行执行

### 7.2 执行模式选择

**1. executing-plans skill** - 执行计划（批量模式）
- **特点**：在当前会话批量执行，带检查点审查
- **适用**：任务较少、需要快速迭代

**2. subagent-driven-development skill** - 子代理驱动开发
- **特点**：每个任务派发新子代理，任务间两阶段审查
- **适用**：任务较多、需要独立上下文
- **优势**：快速迭代、独立上下文、逐任务验证

### 7.3 技能扩展模式

**skill-creator skill** - 技能创建器
- **用途**：
  - 从头创建新技能
  - 编辑或优化现有技能
  - 运行评估测试技能
  - 基准测试技能性能
  - 优化技能描述以提高触发准确性

---

这个流程总结涵盖了从需求分析到最终发布的完整开发周期，每个阶段都有明确的任务、工具和产出。通过Agent Skills的辅助，可以大大提高开发效率和代码质量，但需要注意：

1. **验证Agent的执行结果**：使用 verification-before-completion 确保真实完成
2. **遵循TDD原则**：先写测试，看它失败，再写代码
3. **系统化思考**：使用 systematic-debugging 避免表面修复
4. **设计先行**：使用 brainstorming 确保设计正确再编码
5. **整合验证**：整合到04-正式发行版时必须验证原有功能未被修改、新增功能正确集成、无功能冲突
