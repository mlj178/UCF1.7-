# Universal-Dear-ImGui-Hook 使用教程

## 目录

- [一、项目概述](#一项目概述)
- [二、文件结构](#二文件结构)
- [三、快速使用](#三快速使用)
- [四、编译环境](#四编译环境)
- [五、常见问题](#五常见问题)
- [六、技术原理](#六技术原理)
- [七、ESP 透视功能](#七esp-透视功能)
- [八、项目当前状态](#八项目当前状态)

---

## 一、项目概述

本项目是一个 **DirectX ImGui 注入框架**，用于在 DirectX 游戏中叠加 ImGui 界面。

**适用场景**：
- 32位 DirectX 9/10/11/12 游戏
- 需要在游戏内显示自定义菜单/调试界面

**本项目已适配**：
- UnityCrossFire（32位 Unity IL2CPP 游戏，DirectX 11）

**核心功能**：
- ✅ ImGui 菜单（INSERT 键打开/关闭）
- ✅ DirectX 11 Hook（拦截 Present 函数）
- ✅ ESP 透视方框（hitboxes + neck/spine/helmet 方案）
- ✅ 玩家检测（GameManager.allPlayers 访问）
- ✅ 坐标转换（WorldToScreen 投影）
- ✅ 跨局清理（Session Epoch + Hook回调）
- ✅ 线程同步（CRITICAL_SECTION）
- ✅ 指针过期机制（5秒超时自动清理）

---

## 二、文件结构

```
Universal-Dear-ImGui-Hook/
├── Release/
│   └── Universal-ImGui-Hook.dll      # 最终 DLL（597 KB）
├── esp/                              # ESP 功能模块
│   ├── esp_common.h                  # 通用定义
│   ├── coord_converter.h/cpp         # 坐标转换
│   ├── transform_helper.h/cpp        # Transform 辅助
│   ├── il2cpp_bridge.h/cpp           # IL2CPP 桥接（RVA常量、偏移定义、函数调用封装）
│   ├── game_manager.h/cpp            # 游戏管理器
│   └── esp_renderer.h/cpp            # ESP 渲染器
├── imgui/                            # ImGui 库
├── minhook/                          # MinHook 库
├── dllmain.cpp                       # DLL 入口
├── d3d11hook.cpp                     # DirectX 11 Hook
├── menu.cpp                          # ImGui 菜单
├── build_msvc.bat                    # 编译脚本
├── inject.exe                        # 注入器
└── 使用教程.md                       # 本文档
```

### 2.1 文件功能详解

#### 核心文件（根目录）

| 文件名 | 类型 | 功能说明 |
|--------|------|----------|
| `dllmain.cpp` | C++ 源码 | **DLL 入口点**，负责初始化 Hook、加载 ImGui、设置 DirectX Hook 优先级 |
| `d3d9hook.cpp` | C++ 源码 | **DirectX 9 Hook 实现**，拦截 D3D9 设备创建和渲染调用 |
| `d3d10hook.cpp` | C++ 源码 | **DirectX 10 Hook 实现**，拦截 D3D10 设备创建和渲染调用 |
| `d3d11hook.cpp` | C++ 源码 | **DirectX 11 Hook 实现**，拦截 D3D11 设备创建和渲染调用（本项目主要使用） |
| `d3d12hook.cpp` | C++ 源码 | **DirectX 12 Hook 实现**，拦截 D3D12 设备创建和渲染调用 |
| `hooks.cpp` | C++ 源码 | **其他 Hook 实现**，包括窗口过程 Hook、Present Hook 等 |
| `inputhooks.cpp` | C++ 源码 | **输入 Hook**，拦截键盘输入，处理 INSERT 键打开菜单 |
| `mousehooks.cpp` | C++ 源码 | **鼠标 Hook**，拦截鼠标输入，处理菜单交互 |
| `menu.cpp` | C++ 源码 | **ImGui 菜单实现**，定义菜单界面、控件、配置选项 |
| `globals.cpp` | C++ 源码 | **全局变量定义**，存储菜单状态、配置参数等全局数据 |
| `namespaces.h` | C++ 头文件 | **命名空间定义**，定义项目使用的命名空间 |
| `stdafx.h` | C++ 头文件 | **预编译头**，包含常用头文件，加速编译 |

#### 注入相关文件

| 文件名 | 类型 | 功能说明 |
|--------|------|----------|
| `inject.cs` | C# 源码 | **注入器源码**，负责将 DLL 注入到目标进程 |
| `inject.exe` | 可执行文件 | **编译后的注入器**（32位），实际使用的注入工具 |
| `inject_log.txt` | 日志文件 | **注入日志**，记录注入过程中的错误和状态 |
| `run_inject.bat` | 批处理脚本 | **快捷启动脚本**，自动注入到 UnityCrossFire 进程 |

#### 编译相关文件

| 文件名 | 类型 | 功能说明 |
|--------|------|----------|
| `Universal-ImGui-Hook.sln` | VS 解决方案 | **Visual Studio 解决方案文件**，用于 VS IDE 打开项目 |
| `Universal-ImGui-Hook.vcxproj` | VS 项目文件 | **Visual Studio 项目文件**，定义编译配置、源文件列表 |
| `Universal-ImGui-Hook.vcxproj.filters` | VS 过滤器 | **VS 项目过滤器**，定义解决方案资源管理器中的文件分组 |
| `build_msvc.bat` | 批处理脚本 | **Visual Studio 编译脚本**，使用 MSBuild 编译 DLL |
| `build_mingw.bat` | 批处理脚本 | **MinGW 编译脚本**，使用 GCC 编译 DLL |

#### ESP 功能模块（esp/ 目录）

| 文件名 | 类型 | 功能说明 |
|--------|------|----------|
| `esp_common.h` | C++ 头文件 | **ESP 通用定义**，定义 Vector2/Vector3、内存读取函数、指针验证等基础功能 |
| `coord_converter.h` | C++ 头文件 | **坐标转换头文件**，声明 WorldToScreen、IsOnScreen 等坐标转换函数 |
| `coord_converter.cpp` | C++ 源码 | **坐标转换实现**，实现 3D 世界坐标到 2D 屏幕坐标的投影转换 |
| `transform_helper.h` | C++ 头文件 | **Transform 辅助头文件**，声明骨骼数据获取、碰撞盒数据获取等函数 |
| `transform_helper.cpp` | C++ 源码 | **Transform 辅助实现**，实现骨骼坐标读取、碰撞盒包围盒计算、helmet 补充点逻辑 |
| `il2cpp_bridge.h` | C++ 头文件 | **IL2CPP 桥接头文件**，定义关键偏移常量（RVA）、函数地址、结构体偏移 |
| `il2cpp_bridge.cpp` | C++ 源码 | **IL2CPP 桥接实现**，实现 Unity IL2CPP 函数调用（GetTransform、GetPosition 等） |
| `game_manager.h` | C++ 头文件 | **游戏管理器头文件**，声明玩家列表管理、Session Epoch、线程同步等函数 |
| `game_manager.cpp` | C++ 源码 | **游戏管理器实现**，实现玩家缓存、跨局污染防护、Hook 回调处理 |
| `esp_renderer.h` | C++ 头文件 | **ESP 渲染器头文件**，声明 ESP 绘制函数（方框、骨骼、血条等） |
| `esp_renderer.cpp` | C++ 源码 | **ESP 渲染器实现**，实现 ESP 绘制逻辑，调用 TransformHelper 获取数据并渲染 |

#### 第三方库

| 目录/文件 | 说明 |
|----------|------|
| `imgui/` | **ImGui 库**，提供即时模式 GUI 功能，用于绘制菜单和 ESP 界面 |
| `minhook/` | **MinHook 库**，提供 API Hook 功能，用于拦截 DirectX 函数 |

#### 输出目录

| 目录 | 说明 |
|------|------|
| `Release/` | **最终输出目录**，包含编译生成的 DLL 文件 |
| `Universa.1C015F7C/` | **VS 编译中间文件**，包含 .obj 目标文件、.pdb 调试符号、增量链接文件 |

---

## 三、快速使用

### 步骤 1：启动游戏
```
启动目标游戏（如 UnityCrossFire.exe）
```

### 步骤 2：注入 DLL
```powershell
# 方法 A：双击运行
run_inject.bat

# 方法 B：命令行运行
inject.exe

# 方法 C：指定进程名和 DLL 路径
inject.exe UnityCrossFire "完整路径\Universal-ImGui-Hook.dll"
```

### 步骤 3：打开菜单
```
游戏中按 INSERT 键打开/关闭 ImGui 菜单
```

---

## 四、编译环境

### 4.1 已安装的编译器

#### Visual Studio Community 2022 ⭐ 推荐 ⭐

| 项目 | 信息 |
|------|------|
| **版本** | Community 2022 |
| **安装路径** | `D:\VS2022` |
| **MSBuild路径** | `D:\VS2022\MSBuild\Current\Bin\MSBuild.exe` |
| **MSVC版本** | 14.44.35207 |
| **支持架构** | x86 (32位), x64 (64位) |
| **Windows SDK** | 10.0.26100.0 |

**特点**：
- ✅ 完整的 C++ 开发环境
- ✅ 最佳优化效果
- ✅ 生成 DLL 体积较小（约 597 KB）
- ✅ 完善的调试支持

#### MinGW GCC 15.2.0

| 项目 | 信息 |
|------|------|
| **版本** | GCC 15.2.0 |
| **安装路径** | `D:\Program Files\mingw32` |
| **架构** | i686-win32-dwarf (32位) |
| **C++标准** | C++17 |

**特点**：
- ✅ 轻量级编译器
- ✅ 无需安装 Visual Studio
- ⚠️ 生成 DLL 体积较大（约 1646 KB）

### 4.2 编译器对比

| 编译器 | DLL大小 | 优化效果 | 编译速度 | 推荐度 |
|--------|---------|----------|----------|--------|
| **Visual Studio 2022** | ~597 KB | 最佳 | 快 | ⭐⭐⭐⭐⭐ |
| **MinGW GCC** | ~1646 KB | 一般 | 快 | ⭐⭐⭐ |

### 4.3 编译方法

#### 方法 A：使用 Visual Studio（推荐）

**快速编译**：
```powershell
.\build_msvc.bat
```

**编译输出**：
```
✅ 编译成功！
输出: Release\Universal-ImGui-Hook.dll
大小: ~597 KB（包含完整ESP功能）
```

#### 方法 B：使用 MinGW（GCC）

**快速编译**：
```powershell
.\build_mingw.bat
```

**编译输出**：
```
✅ 编译成功！
输出: Release\Universal-ImGui-Hook.dll
大小: ~1646 KB
```

---

## 五、常见问题

### Q1：注入后游戏崩溃

**原因**：
- DLL 位数与游戏不匹配
- DirectX 版本不匹配
- 缺少运行时依赖

**解决**：
```
1. 确认游戏位数，编译对应位数的 DLL
2. 确认游戏使用的 DirectX 版本
3. 安装 Visual C++ Redistributable
```

### Q2：注入成功但无菜单

**原因**：
- DirectX Hook 未生效
- 游戏使用了非标准渲染方式

**解决**：
```
1. 检查游戏是否加载了 d3d11.dll/d3d9.dll
2. 尝试不同的 Hook 顺序
3. 使用 Process Hacker 确认 DLL 已加载
```

### Q3：注入器报错 "未找到 LoadLibraryA"

**原因**：注入器位数与游戏不匹配

**解决**：
```
32位游戏 → 使用 32位注入器（/platform:x86）
64位游戏 → 使用 64位注入器（/platform:x64）
```

### Q4：模块数很少（7个）正常吗？

**正常！**

```
64位 Windows 查看器无法直接看到 32位进程的真实模块
这是 WOW64 模拟层的正常现象
只要注入成功且菜单出现，就说明一切正常
```

### Q5：编译报错 LNK1104: 无法打开文件

**原因**：DLL 被游戏进程占用

**解决**：
```
关闭游戏进程，重新编译
```

---

## 六、技术原理

### 6.1 注入原理

```
┌─────────────────────────────────────────────────────────┐
│ 注入流程                                                 │
├─────────────────────────────────────────────────────────┤
│ 1. 找到目标进程                                          │
│ 2. 获取 kernel32.dll 在目标进程中的基址                  │
│ 3. 计算 LoadLibraryA 的 RVA（相对虚拟地址）              │
│ 4. 在目标进程分配内存，写入 DLL 路径                     │
│ 5. 创建远程线程，执行 LoadLibraryA                       │
│ 6. DLL 加载，DllMain 执行 Hook 初始化                    │
└─────────────────────────────────────────────────────────┘
```

### 6.2 关键技术点

**RVA 计算**（解决跨位数问题）：
```csharp
// 本地 kernel32.dll 地址
IntPtr localKernel32 = GetModuleHandle("kernel32.dll");
// 本地 LoadLibraryA 地址
IntPtr localLoadLibrary = GetProcAddress(localKernel32, "LoadLibraryA");
// 计算 RVA
long rva = localLoadLibrary.ToInt64() - localKernel32.ToInt64();
// 目标进程的 LoadLibraryA
IntPtr remoteLoadLibrary = new IntPtr(remoteKernel32.ToInt64() + rva);
```

**DirectX Hook**：
```cpp
// Hook Present 函数
oPresent = (Present_t)SwapChain->lpVtbl[8];  // DXGI SwapChain 虚函数表
MH_CreateHook(oPresent, hkPresent, reinterpret_cast<LPVOID*>(&oPresent));
MH_EnableHook(oPresent);
```

---

## 七、ESP 透视功能

### 7.1 ESP 代码位置

ESP（透视）功能代码位于 `esp/` 文件夹：

```
esp/
├── esp_common.h          # 通用定义、安全内存访问函数
├── il2cpp_bridge.h/cpp   # IL2CPP API 封装
├── game_manager.h/cpp    # GameManager 访问（玩家列表、队伍信息）
├── transform_helper.h/cpp # Transform 组件访问（坐标获取）
├── coord_converter.h/cpp # 坐标转换（3D → 2D）
└── esp_renderer.h/cpp    # ESP 渲染器（方框绘制）
```

### 7.2 ESP 功能原理

```
┌─────────────────────────────────────────────────────────┐
│ ESP 渲染流程                                             │
├─────────────────────────────────────────────────────────┤
│ 1. GameManager.GetAllPlayers() - 获取所有玩家            │
│ 2. TransformHelper.GetHitboxData() - 获取碰撞盒包围盒    │
│    ├─ hitboxes数组遍历（身体碰撞盒）                     │
│    ├─ neck/spine/spine1固定补充点（头部和身体稳定点）    │
│    └─ helmet collider补充点（头盔碰撞盒）                │
│ 3. CoordConverter.WorldToScreen() - 转换为屏幕坐标       │
│ 4. ESPRenderer.DrawBox() - 绘制方框                      │
└─────────────────────────────────────────────────────────┘
```

**当前实现方案**：Hitbox-based ESP

| 特性 | 说明 |
|------|------|
| **数据源** | CharacterModel.hitboxes（Transform数组） |
| **补充点** | neck + spine + spine1 + helmet collider |
| **优势** | 自适应不同角色体型，无需固定身高参数 |
| **日志** | esp_debug.log（记录每次渲染的详细数据） |

**方框计算逻辑**：
```cpp
// 1. 遍历hitboxes数组，获取所有碰撞盒Transform
for (int i = 0; i < hitboxCount; i++) {
    Transform* tf = hitboxes[i];
    Vector3 worldPos = Transform.get_position(tf);
    Vector2 screenPos = WorldToScreen(worldPos);
    // 更新包围盒边界
    left = min(left, screenPos.x);
    right = max(right, screenPos.x);
    top = min(top, screenPos.y);
    bottom = max(bottom, screenPos.y);
}

// 2. 强制加入neck/spine/spine1作为固定补充点
addTransformPoint(neck);    // 头部顶部
addTransformPoint(spine);   // 身体中轴
addTransformPoint(spine1);  // 身体稳定点

// 3. 加入helmet collider作为头部补充点
helmetCollider = helmet + 0x10;
addTransformPoint(helmetCollider.transform);

// 4. 计算最终方框尺寸
width = right - left;
height = bottom - top;
```

### 7.3 关键偏移常量

**CharacterModel 字段偏移**：

| 字段 | 偏移 | 说明 |
|------|------|------|
| characterAnimator | 0x48 | Animator 组件 |
| spine | 0x5C | Transform（身体） |
| spine1 | 0x60 | Transform（身体） |
| neck | 0x64 | Transform（颈部） |
| helmet | 0x6C | Helmet 对象 |
| hitboxes | 0x74 | Transform[]（碰撞盒数组） |

**Helmet 字段偏移**：

| 字段 | 偏移 | 说明 |
|------|------|------|
| helmetCollider | 0x10 | Collider 指针 |

---

## 八、项目当前状态

### 8.1 最新编译信息

| 项目 | 信息 |
|------|------|
| **编译时间** | 2026-06-05 11:19 |
| **DLL大小** | 596,992 字节（~597 KB） |
| **编译器** | Visual Studio 2022 (MSVC 14.44) |
| **配置** | Release x86 |
| **输出位置** | `Release\Universal-ImGui-Hook.dll` |

### 8.2 功能实现状态

| 功能 | 状态 | 说明 |
|------|------|------|
| **ImGui 菜单** | ✅ 完成 | INSERT 键打开/关闭 |
| **DirectX 11 Hook** | ✅ 完成 | Hook Present 函数 |
| **ESP 方框** | ✅ 完成 | hitboxes + neck/spine/helmet 方案 |
| **玩家检测** | ✅ 完成 | GameManager.allPlayers 访问 |
| **坐标转换** | ✅ 完成 | WorldToScreen 投影 |
| **跨局清理** | ✅ 完成 | Session Epoch + Hook回调 |
| **线程同步** | ✅ 完成 | CRITICAL_SECTION |
| **指针过期** | ✅ 完成 | 5秒超时自动清理 |
| **调试日志** | ✅ 完成 | esp_debug.log |

### 8.3 已解决的问题

| 问题 | 原因 | 解决方案 | 状态 |
|------|------|---------|------|
| **跨局指针污染** | 房间切换后仍保留上一局玩家指针 | Session Epoch + 归属校验 + Hook回调清理 | ✅ 已解决 |
| **方框位置偏移** | 使用characterContainer + 固定身高 | 改用hitboxes + neck/spine/helmet补充点 | ✅ 已解决 |
| **方框顶部缺失** | hitboxes不包含头部 | 强制加入neck和helmet collider | ✅ 已解决 |
| **多线程数据竞争** | 并发访问玩家列表 | CRITICAL_SECTION线程同步 | ✅ 已解决 |
| **指针长期积累** | 无过期清理机制 | 时间戳过期机制（5秒超时） | ✅ 已解决 |

### 8.4 当前已知问题

| 问题 | 现象 | 优先级 |
|------|------|--------|
| 方框尺寸波动大 | 部分角色方框过窄或过大 | 中 |
| helmet加入率低 | 约60%角色无helmet补充点 | 低 |

### 8.5 已知限制

| 限制 | 说明 | 影响 |
|------|------|------|
| 仅支持 DirectX 11 | 未实现 D3D9/10/12 Hook 优先级调整 | 需要手动修改 dllmain.cpp |
| 32位游戏 | 未适配 64位游戏 | 需要重新编译 x64 版本 |
| Unity IL2CPP | 不支持 Mono 游戏 | 需要适配不同的内存布局 |

### 8.6 下一步优化方向

| 优化项 | 优先级 | 说明 |
|--------|--------|------|
| 方框尺寸异常过滤 | 高 | 增加 width>500 或 height>400 的异常方框过滤 |
| helmet 加入率提升 | 中 | 优化 helmet collider 读取逻辑 |
| 窄框处理优化 | 中 | 调整最小宽度比例（当前 0.35，建议 0.45） |
| 性能优化 | 低 | 减少每帧日志输出频率 |

### 8.7 常见错误代码

| 错误代码 | 含义 | 解决方案 |
|----------|------|---------|
| 0xC0000005 | 访问违规 | 检查内存访问代码，添加 SEH 异常处理 |
| 0xC0000409 | 栈缓冲区溢出 | 检查数组访问，确认索引范围 |
| 0xC000001D | 非法指令 | 检查函数指针调用，确认地址正确 |
| LNK1104 | 无法打开文件 | 关闭游戏进程，重新编译 |

---

**项目状态**：✅ 功能完整，可用于实际游戏测试

**最后更新**：2026-06-05
