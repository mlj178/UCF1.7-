# 调整伤害倍率 - 简要说明

## 文件

| 文件 | 说明 |
| --- | --- |
| `AAAAA-damage_multiplier_min.js` | Frida 核心脚本 |
| `AAAAA-damage_multiplier_ui.py` | CustomTkinter 单功能 UI |
| `test_damage_multiplier_static.py` | 静态验证测试 |
| `伤害来源分类与倍率可行性.md` | 伤害来源分类和可行性研究 |

## 默认行为

- 默认倍率：`2.0x`。
- 默认放大玩家造成的输出伤害。
- 默认覆盖 `Gun`、`Knife`、`Grenade`，并允许 `Other` 类型特殊技能伤害。
- 默认启用导弹和炮台 owner 映射，用于 RPG、榴弹、手雷、机枪守卫等特殊来源。

## 生效范围，说人话版

这个功能调整的不是“全场伤害”，而是“你自己打出去的伤害”。

- 你是人类：调整你作为人类打出去的伤害。
- 你是幽灵：只要游戏这次伤害能识别为你造成，并且伤害类型在支持范围内，也会调整。
- 其他真实玩家：不会放大。
- Bot / 人机：不会放大。
- 敌人打你：不会放大。

支持的伤害类型：

- 枪械伤害：支持。
- 近战 / 刀伤害：支持。
- 手雷 / 爆炸类伤害：支持。
- `Other` 类型特殊伤害：默认允许，比如部分英雄技能或特殊武器伤害，但不同武器需要实测确认。
- RPG、AT4、榴弹、炮台、机枪守卫这类“伤害源不是玩家本体”的情况：脚本会记录 owner，只要能映射回本地玩家，就会调整。

不调整的情况：

- 找不到本地玩家归属的伤害：不调整。
- 其他玩家、Bot、人机造成的伤害：不调整。
- 无效伤害、0 伤害、负数伤害：不调整。
- 不在 `Gun / Knife / Grenade / Other` 里的未知伤害类型：不调整。

简单理解：这不是阵营倍率，也不是全局倍率；它只尽量调整“本地玩家自己造成的有效伤害”。

## 核心原理

脚本 Hook `Entity.OnEntityHurt` 和 `Player.OnEntityHurt`，在 x86 cdecl 栈上定位按值传入的 `DamageEventData`，只修改 `damage` 字段：

| 字段 | 偏移 |
| --- | ---: |
| `attacker` | `0x00` |
| `victim` | `0x04` |
| `damage` | `0x08` |
| `type` | `0x34` |
| `damageTag` | `0x38` |
| `wpnIndex` | `0x3C` |
| `wpnSprIndex` | `0x40` |
| `ignoreDmgRate` | `0x44` |

为避免 `Player.OnEntityHurt` 调用基类时重复放大，脚本在 Player 入口期间记录 `playerHurtDepth`，同一调用链内的 `Entity.OnEntityHurt` 只跳过不二次修改。

## 需要实测关注

- 机械英雄炮台：确认 `SentryGun.SetData` owner 映射能命中。
- RPG/AT4/榴弹枪：确认 missile owner 映射能命中。
- GhostBlade、MasterHero、BoxingKing 等特殊武器：观察 `damageTag` 和 `DamageType.Other` 是否符合预期。

## 验证命令

```powershell
python -m unittest "05-正式功能\30-增加伤害倍率\test_damage_multiplier_static.py" -v
python -m py_compile "05-正式功能\30-增加伤害倍率\AAAAA-damage_multiplier_ui.py"
node --check "05-正式功能\30-增加伤害倍率\AAAAA-damage_multiplier_min.js"
```
