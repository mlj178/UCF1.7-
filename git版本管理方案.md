## 推荐的分支策略

### 分支结构（保持简单）

```
main          ← 稳定版本（可发布的版本）
  └─ dev      ← 开发分支（日常开发用）
```

**只需要 2 个分支就够了！**

---

## 日常操作流程

### 1. 什么时候提交（commit）？

| 时机           | 示例                             |
| -------------- | -------------------------------- |
| 完成一个小功能 | 添加了无限弹药功能               |
| 修复了一个 Bug | 修复了闪退问题                   |
| 修改了配置文件 | 调整了内存地址                   |
| **频率**       | 每次有意义的改动都提交，不用攒着 |

### 2. 什么时候推送到远程（push）？

| 时机           | 说明                      |
| -------------- | ------------------------- |
| 每天下班前     | 备份到码云，防止丢失      |
| 完成重要功能后 | 及时备份                  |
| **频率**       | 一天 2-3 次，符合你的需求 |

### 3. 什么时候合并到 main？

| 时机           | 说明             |
| -------------- | ---------------- |
| 测试版稳定后   | 可以发布时       |
| 完成一个大版本 | 比如 v1.0 → v1.1 |

---

## 针对测试版文件夹的建议

**问题：** 测试版都是小功能，频繁分支很繁琐

**解决方案：**

### 方案 A：直接在 dev 分支开发（推荐）

```
dev 分支
  ├─ 测试版/无限时间/
  ├─ 测试版/快刀/
  └─ 正式发行版/
```

- 所有测试功能都放在 `测试版/` 文件夹下
- 功能稳定后，移动到 `正式发行版/`
- **不需要为每个小功能创建分支**

---

## 版本回退方案

### 场景 1：回退到某个历史版本

```bash
# 查看提交历史

# 回退到指定提交（保留历史记录）

# 或者硬回退（丢失之后的提交，谨慎使用）
```

### 场景 2：用标签快速回退

```bash
# 查看已有标签


# 回退到某个版本

```

---

## 简化版日常操作清单

```
早上开始工作：
  1. git pull origin dev          ← 拉取最新代码

工作中：
  2. 修改代码
  3. git add .
  4. git commit -m "描述改动"      ← 每次有意义的改动都提交

中午/下午/下班前：
  5. git push origin dev          ← 推送到码云备份

发布新版本时：
  6. git checkout main
  7. git merge dev
  8. git tag -a v1.2 -m "版本说明"
  9. git push origin main --tags
  10. git checkout dev            ← 回到开发分支
```

---

## 总结

| 操作          | 频率             | 分支 |
| ------------- | ---------------- | ---- |
| commit        | 每次有意义的改动 | dev  |
| push          | 每天 2-3 次      | dev  |
| merge 到 main | 发布新版本时     | main |
| 打标签        | 发布新版本时     | main |
| 创建功能分支  | 仅用于大功能改动 | 临时 |

**核心原则：**
- 日常开发只用 `dev` 分支
- 测试版放在文件夹里，不用分支
- `main` 分支只放稳定版本
- 每天推送 2-3 次到码云备份

这样既简单又能满足你的备份和回退需求！
