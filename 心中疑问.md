## 一、你的脚本中使用了哪些修改方法？

**Hook 返回值**（`Interceptor.attach` 修改 `retval`，如 `force_pick.js`）

**函数完全替换**（`Interceptor.replace` + `NativeCallback`，如多数修改器脚本）

**直接内存读写**（`Memory.writeInt` / `writeFloat` 修改对象的字段，后座力清零、时间锁定）

**补丁机器码**（NOP / 修改 .text 段）

**辅助调用**（`NativeFunction` 用来调用游戏内置函数，如 `isMyPlayer`）



## 如何稳定使用 `NativeFunction`（避免崩溃指南）

1. **明确真实的函数签名**
   使用 IDA 或 Il2CppInspector 分析，确认函数是否带有隐藏 `MethodInfo*` 参数。例如 IL2CPP 的实例方法通常签名为：



**尽可能避免在 Hook 里调用复杂函数**，优先直接读字段。
比如判断 `isMyPlayer`，可以自己通过对比对象指针和全局 `myPlayer` 实例来实现，而不是调函数。

**优先选择 `Interceptor.replace` + 直接操作字段**（像你的无后座力和 ConsumeAmmo 方案），这比调用其他游戏函数要稳定得多。



你的脚本用到了 Frida 几乎所有的核心修改手段。`NativeFunction` 崩溃的根源在于**调用约定不明确、参数/指针无效**。在 IL2CPP 逆向不彻底的情况下，尽量用"改返回值 + 写内存"的组合代替"调用游戏内部函数"。

---







# ClientData.nickname 定位与修改总结

## 一、问题背景

- **目标**：定位并修改 Il2Cpp 游戏中 `ClientData` 实例的 `nickname` 字段
- **已知**：`ClientData` 实例地址 `0x2DDA058`，`nickname` 字段偏移 `0x10`
- **困惑**：
  1. 搜实例地址 `0x2DDA058` 出现 5 个引用结果，分不清哪个是实例本体
  2. 按偏移算出的 `0x2DDA068` 搜不到任何结果
  3. 跳转到字符串地址后，中文显示乱码

---

## 二、核心概念

| 概念 | 说明 |
|:--|:--|
| `0x2DDA058`（实例地址） | **对象本体起始地址**。搜到的 5 个结果是别的结构在引用这个对象（存了指向它的指针） |
| `0x2DDA068`（nickname 字段地址） | **存指针的变量地址**，不是文本本身。该地址的 4 字节指向真正的 String 对象 |
| Il2Cpp String 编码 | 默认 **UTF-16LE（双字节）**，CE 默认按 ASCII 显示 → 中文乱码 |

### Il2Cpp String 对象内存布局（32位）

| 偏移 | 大小 | 内容 |
|:--|:--|:--|
| +0x00 | 4B | `klass`（对象头，指向类信息） |
| +0x04 | 4B | `monitor`（对象头，同步锁） |
| **+0x08** | **4B** | **`m_stringLength`**（字符个数，不是字节数） |
| **+0x0C** | **N×2B** | **实际文本（UTF-16LE）** |

### ClientData 对象内存布局（32位）

| 偏移 | 大小 | 字段 |
|:--|:--|:--|
| +0x00 | 4B | vtable 指针（对象头） |
| +0x04 | 4B | sync block（对象头） |
| +0x08 | 4B | `defaultWpnBagID` |
| +0x0C | 4B | `wpnBags`（指针） |
| **+0x10** | **4B** | **`nickName`（String 指针）** |
| +0x14 | 4B | `level` |
| ... | ... | ... |

**结论**：`nickName` 地址 = `0x2DDA058 + 0x10 = 0x2DDA068` ✅

---

## 三、为什么搜不到 `0x2DDA068`？

原因是**类型不同，引用方式不同**：

| 操作 | 搜的是什么 | 结果 | 原因 |
|:--|:--|:--|:--|
| 搜 `0x2DDA058` | 谁存了这个值 | 5 个结果 | 别处存了指向此实例的指针，值就是 `0x2DDA058` |
| 搜 `0x2DDA068` | 谁存了这个值 | **无结果** | 没有人存 `0x2DDA068` 这个值，代码通过 `实例地址+0x10` 计算访问 |

---

## 四、CE 操作完整步骤

### Step 1：定位实例本体

1. CE 搜索 `0x2DDA058`（4 Bytes），得到若干引用地址
2. 右键任一结果 → **Browse this memory region**
3. 顶部地址栏输入 `0x2DDA058` 回车 → 看到实例本体的内存数据

### Step 2：取出 String 指针

1. 在 `0x2DDA058` 行的内存中，看偏移 **+0x10** 处的 4 字节

2. 小端序读出指针值（比如 `23 BA 88 70` → `0x23BA8870`），这就是 String 对象地址

   ![image-20260514164340177](C:\Users\17242\AppData\Roaming\Typora\typora-user-images\image-20260514164340177.png)

   ![image-20260514164541264](C:\Users\17242\AppData\Roaming\Typora\typora-user-images\image-20260514164541264.png)

### Step 3：查看 nickname 内容

| 方法 | 操作 |
|:--|:--|
| **方式 A（内存浏览）** | 地址栏输入 String 地址（如 `0x23BA8870`）→ 右键 → **Display Type → Unicode String** |

> **乱码解决**：必须在内存视图右键切换编码为 **Unicode/UTF-16LE**，CE 默认 ASCII 无法显示中文。

---

## 五、注意事项

| 注意点 | 说明 |
|:--|:--|
| **地址会变** | 游戏重启后 `ClientData` 实例地址重新分配，需重新定位 |
| **服务端校验** | 部分游戏对 `nickname` 做服务端校验，本地修改仅本地显示，切地图/重登可能还原 |
| **String 只读区** | 某些游戏的 String 对象在只读内存页，需先 `VirtualProtect` 修改保护属性 |
| **崩溃风险** | 修改字符串时不要破坏相邻字段的内存，操作前备份进程 |
| **简化思路** | 如果只是实现功能，优先考虑 Hook `get_nickName()` 返回值，比直接写内存更稳定 |
