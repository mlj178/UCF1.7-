# UCF 1.7 整合包问题追踪表

> **创建日期**: 2026-06-19 | **数据来源**: GLM5.2 静态分析 + 源码核实
> **勾选规则**: 解决一项打 ✓，更新"完成日期"列

---

## 阶段 0：止血 + 可观测性（零结构改动，先让软件能用）

| ✓ | 编号 | 问题 | 严重度 | 修复内容 | 涉及文件 | 验证方法 | 完成日期 |
|---|------|------|--------|----------|----------|----------|----------|
| | 0-01 | `battle_round_always` 默认开启导致闪退 | 🔴致命 | 默认值改为 `false` | `data/feature_state.json` | 删除 json 重启，确认默认为 false | |
| | 0-02 | ESP DLL 无条件注入导致卡顿 | 🔴致命 | 改为条件注入：仅用户开启 `esp_box` 时才注入 | `core/game_session_manager.py` 状态机 | 重命名 `plugins/universal_hook` 目录后卡顿消失 | |
| ✓ | 0-03 | DLL 死循环轮询 IL2CPP（6-7次/秒） | 🔴致命 | Python 端 ping 加 10s 节流；universal 重连间隔从 5s 改 30s；READY 态步进从 2s 改 5s | `core/universal_hook_manager.py` + `core/game_session_manager.py` | `esp_debug.log` 重试频率降至合理范围 | 2026-06-19 |
| ✓ | 0-04 | 持续管道 I/O 占用（每 0.5s ping） | 🟠严重 | READY 态步进间隔从 2s 改为 5s；ping 加 10s 节流；universal retry 从 5s 改 30s | `core/game_session_manager.py` + `core/universal_hook_manager.py` | 任务管理器观察 CPU 占用 | 2026-06-19 |
| ✓ | 0-05 | 无标准 logging，崩溃后日志全丢 | 🟠严重 | 引入 Python 标准 `logging`，日志落盘到 `logs/game_modifier.log`，加 `trace_id`，5MB 轮转保留 3 份 | 新建 `core/log_manager.py` + 改 `core/frida_manager.py` + `ui/app.py` | 崩溃后 `logs/` 有完整日志可追溯 | 2026-06-19 |
| | 0-06 | `timescale` 默认开启影响游戏体验 | �中等 | 默认值改为 `false` | `data/feature_state.json` | 删除 json 重启，确认默认为 false | |

**阶段 0 产出验证**：用户反馈卡顿/闪退消失；日志文件能定位崩溃前最后一步。

---

## 阶段 1：建安全网——行为测试 + Fake 游戏环境（动结构之前必须有）

| ✓ | 编号 | 问题 | 严重度 | 修复内容 | 涉及文件 | 验证方法 | 完成日期 |
|---|------|------|--------|----------|----------|----------|----------|
| | 1-01 | 无测试文件 | 🟠严重 | 建立测试目录 `tests/`，配置 `pytest` + `conftest.py` | 新建 `tests/` 目录 | `pytest` 可运行 | |
| | 1-02 | 无 Fake 后端，测试必须连真游戏 | 🟠严重 | 写 `FakeFrida`：记录所有 `call_export`/`send_toggle` 调用，不连真游戏 | 新建 `tests/fakes/fake_frida.py` | FakeFrida 能接收并记录调用 | |
| | 1-03 | 无行为测试 | 🟠严重 | 写连接/断连/重连状态机行为测试 | 新建 `tests/test_session.py` | 故意改坏状态机逻辑，测试会红 | |
| | 1-04 | Feature 开关无测试 | 🟠严重 | 写 feature 开关行为测试：给定输入 → 断言 FakeFrida 收到正确调用序列 | 新建 `tests/test_features.py` | 故意改坏一个 feature，测试会红 | |
| | 1-05 | nano4t 模式守卫无测试 | 🟡中等 | 写 nano4t 模式进入/退出/功能守卫行为测试 | 新建 `tests/test_nano4t.py` | 非 nano4t 模式下功能开关被正确拦截 | |
| | 1-06 | 持久化读写无测试 | 🟡中等 | 写 `feature_state.json` 读写行为测试 | 新建 `tests/test_persistence.py` | 重启后状态与退出时一致 | |
| | 1-07 | `pytest --cov` 无覆盖率 | �中等 | 配置覆盖率，确保核心模块被覆盖 | `pytest.ini` / `pyproject.toml` | `pytest --cov` 跑出覆盖率报告 | |

**阶段 1 产出验证**：`pytest --cov` 能跑出覆盖率；故意改坏逻辑，测试会红。

---

## 阶段 2：拆黑盒——三层分离（绞杀者模式，逐个迁移）

| ✓ | 编号 | 问题 | 严重度 | 修复内容 | 涉及文件 | 验证方法 | 完成日期 |
|---|------|------|--------|----------|----------|----------|----------|
| | 2-01 | Feature 系统名存实亡，UI 硬编码 16 个开关 | 🟠严重 | 让 `FeatureRegistry` 成为唯一真相源，UI 自动从它生成开关 | `core/feature_registry.py` + `ui/app.py` switch_map | 新增 feature 只需写 Feature 类，不碰 app.py | |
| | 2-02 | app.py 直接 `send_toggle` 旁路 Feature 对象 | 🟠严重 | 所有 toggle 调用走 Feature 对象的 `enable()`/`disable()`，不再直接调 `send_toggle` | `ui/app.py:968-988` | FakeFrida 行为测试全绿 | |
| | 2-03 | 上帝类 app.py（1818+ 行） | 🟠严重 | 三层分离：UI层 → Feature层 → Service层 | `ui/app.py` + 新建 `services/` 目录 | app.py 行数降至 < 800 | |
| | 2-04 | 迁移 Feature 01-10（无限弹药→跳过回合） | 🟡中等 | 逐个迁移，每迁一个先补测试再动代码 | `features/feature_01~10_*.py` + `ui/app.py` | 每迁移一个，pytest 全绿 + 真机验证 | |
| | 2-05 | 迁移 Feature 11-17（自动瞄准→技能无冷却） | 🟡中等 | 同上 | `features/feature_11~17_*.py` + `ui/app.py` | 同上 | |
| | 2-06 | 迁移 Feature 18-20（ESP方框/决战回合/时间加速） | 🟡中等 | 同上，ESP 和决战回合需额外注意 DLL/Frida 双通道 | `features/feature_18~20_*.py` + `ui/app.py` | 同上 | |
| | 2-07 | `battle_round.js` 场景销毁时指针失效闪退 | 🔴致命 | Hook onEnter 加场景有效性检查；退出房间时主动 disable | `scripts/19-battle_round.js` | 退出房间 20 次无闪退 | |
| | 2-08 | DLL 注入时机与渲染线程竞争闪退 | �严重 | 延迟注入至游戏窗口完全初始化后；或改用 Frida `Module.load` | `core/universal_hook_manager.py:_inject()` | 对比注入时机与闪退频率 | |
| | 2-09 | 持久化与运行时状态不同步 | 🟡中等 | 重连后用持久化状态重新 apply 所有 feature；区分"意图状态"和"实际状态" | `ui/app.py` + `core/game_session_manager.py` | 重连后功能状态与上次一致 | |
| | 2-10 | `time_scale.js` 机器码扫描定位风险 | 🟡中等 | 加版本校验；定位失败时安全降级 | `scripts/20-unity_time_acceleration.js` | 游戏更新后 time_scale 安全降级 | |

**阶段 2 产出验证**：每迁移一个 feature，pytest 全绿 + 真机手动验证；app.py 降至 < 800 行。

---

## 阶段 3：治理单例与并发（最难，靠测试网兜底）

| ✓ | 编号 | 问题 | 严重度 | 修复内容 | 涉及文件 | 验证方法 | 完成日期 |
|---|------|------|--------|----------|----------|----------|----------|
| | 3-01 | 单例满天飞（60+ 处 `get_instance()`） | �中等 | 改为构造函数依赖注入，Service 层从外部传入（测试时传 Fake） | 全项目 75 处 `get_instance` | 测试中可注入 Fake 替代真实单例 | |
| | 3-02 | 事件总线每事件开新线程 | 🟡中等 | 改为同步 emit + 主线程队列派发，或线程池复用 | `core/event_bus.py:44` | 快速开关功能 50 次，线程数稳定 | |
| | 3-03 | App 共享状态无锁并发 | 🟡中等 | 共享状态加 `threading.Lock` 或迁到单线程事件循环 | `ui/app.py` `_features` 等实例变量 | 快速重连/切房间 50 次无偶发崩溃 | |
| | 3-04 | `_nano4t_auto_health_bg` 常驻后台 | 🟡中等 | 仅在 nano4t 模式激活时启动，退出时停止 | `ui/app.py:1853` | 非 nano4t 模式下线程不存在 | |
| | 3-05 | `_nano4t_auto_getcurrent_bg` 常驻后台 | 🟡中等 | 同 3-04 | `ui/app.py:1876` | 同 3-04 | |
| | 3-06 | `_monitor_connection` 轮询 | 🟡中等 | 改为事件驱动（连接断开时由 Frida 回调通知） | `ui/app.py:1442` | frida RPC 调用频率显著降低 | |
| | 3-07 | `GameSessionManager._worker_loop` READY 态轮询 | 🟡中等 | READY 态改为事件驱动（进程退出/连接断开时触发） | `core/game_session_manager.py:139` | 任务管理器观察线程活动 | |

**阶段 3 产出验证**：压测（快速开关/重连/切房间）跑 50 次无偶发崩溃。

---

## 进度总览

| 阶段 | 目标 | 总项 | ✓ 已完成 | 剩余 | 进度 |
|------|------|------|----------|------|------|
| 阶段 0 | 止血 + 可观测性 | 6 | 3 | 3 | ██▓░░░ 50% |
| 阶段 1 | 建安全网（测试） | 7 | 0 | 7 | ░░░░░░ 0% |
| 阶段 2 | 拆黑盒（三层分离） | 10 | 0 | 10 | ░░░░░░ 0% |
| 阶段 3 | 治理单例与并发 | 7 | 0 | 7 | ░░░░░░ 0% |
| **合计** | | **30** | **3** | **27** | **█▓░░░░ 10%** |

---

## 变更日志

| 日期 | 操作 | 编号 | 说明 |
|------|------|------|------|
| 2026-06-19 | 创建 | 全部 | 初始版本，按阶段 0→1→2→3 排列，共 30 项待办 |
| 2026-06-19 | 完成 | 0-03 | Python 端 ping 加 10s 节流，universal retry 5s→30s，READY 步进 2s→5s |
| 2026-06-19 | 完成 | 0-04 | READY 态步进间隔 2s→5s，ping 加 10s 节流，universal retry 5s→30s |
| 2026-06-19 | 完成 | 0-05 | 新建 `core/log_manager.py`（标准 logging + RotatingFileHandler + trace_id），改造 `frida_manager.py` 的 LogManager 为桥接，`app.py` 初始化时 setup_logging + 所有 log_message 事件自动落盘 |
