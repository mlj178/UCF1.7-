需求分析，

我的目标是：自瞄：负责把准星对准视野范围内距离准星最近的敌人

请帮我分析这个功能的技术可行性，并给出具体的实现思路。

请帮我将这个大目标拆分为具体步骤：

每个步骤需要说明：

\- 要做什么

\- 预期输出是什么







如何完成上述的需求分析



在 dump.cs 中搜索目标
dump.cs里面的RVA偏移不一定准确，有的需要去IDA验证。尤其是方法的RVA，有的是错误的，需要去IDA验证。

搜索关键词：{关键词}

需要找到：
1. 类名包含 {关键词} 的类
2. 方法名包含 {关键词} 的方法
3. 字段名包含 {关键词} 的字段
4. 列出找到的类、方法、字段的 RVA 地址

请整理成表格形式输出。

我找到了目标类：{类名}

请分析：
1. 这个类继承自谁
2. 画出继承链
3. 找到关键字段的偏移（offset）
4. 找到操作这些字段的方法

请以清晰的格式输出分析结果。





参考已有成功代码提示词

为什么需要参考已有的成功案例代码，因为有的思路是不具备可行性的。参考一下成功案例，避免走错路。

- 查看（正式发行版）文件夹里面的（game_modifier_v1.3.py）的功能，这些功能已经验证可用的代码



请找出与我需求相似的参考代码，并分析：

1. 他们是如何实现类似功能的
2. 使用了哪些关键类和方法
3. 处理了哪些边界情况
4. 有哪些可以复用的代码模式






IDA

已经开启IDA的MCP，请你查询代码。

IDA的 .text是10001000

请执行以下操作：
1. 在 script.json 中找到方法的 RVA 地址
2. 用 IDA MCP 反编译该地址
3. 查看函数的真实流程
4. 确认字段偏移是否正确
5. 找到隐藏的约束条件
6. 看清调用关系

IDA 反编译能做到的 5 件事：
- 看函数的真实流程：decompile_function(地址)
- 确认字段偏移是否正确：反编译里看到 a1 + 216 = 0xD8
- 找到隐藏的约束条件：代码里 if (人数 < 8)
- 看清调用关系：get_callees(地址)
- 验证推测是否正确：把之前猜的逻辑跟反编译对比

请输出反编译结果和分析，验证之前的思路和想法是否正确、可行。








请帮我选择合适的 Frida 修改方式：

| 场景 | 用什么 | 示例 |
|------|--------|------|
| 只看看函数何时被调、传了什么参数 | `Interceptor.attach` | 调试追踪 |
| 阻止原函数，换成自己的逻辑 | `Interceptor.replace` | 无后座力、无限时间 |
| 主动调游戏函数 | `NativeFunction` | 英雄变身、加 Buff |

请分析：
1. 我要拦截什么函数？
2. 原函数还要不要执行？
3. 这个函数每帧都调用吗？

并给出推荐的修改方式。

### 3.3 脚本开发标准提示词

目标：{功能描述}

当前版本：v{版本号}

实现思路：{描述你的思路}

要求：
1. 只对自己生效
2. 退出房间自动清理
3. 不崩溃
4. 全模式适配
5. 区分玩家和Bot

请生成js文件，并告诉我脚本的逻辑和流程。

文件放在（01-正式功能）文件夹下


frida的脚本日志输出要全面，方便调试，找出问题所在。
添加强日志，可以定位具体卡在哪一步，方便调试。














## 四、阶段四：Python 可视化界面开发提示词

### 4.1 生成 Python UI 界面提示词

```
请为这个功能生成一个 Python CustomTkinter 可视化界面。

参考：
- `正式功能\10-跳过回合\AAAAA-skip_round_ui.py`
- `正式功能\09-跳高+失重\AAAAA-gravity_modifier_ui-v2.py`
- `正式功能\08-聚怪\AAAAA-bot_to_spawn_v24.py`

要求：
1. 使用 CustomTkinter 框架，Dark 模式
2. 包含状态栏（连接状态、PID 显示）
3. 包含信息面板（实时数据显示）
4. 包含操作按钮（主按钮 + 辅助按钮）
5. 包含日志区域（实时显示日志）
6. 自动检测游戏进程并连接
7. 使用 threading 后台线程，避免阻塞主线程
8. 使用 self.after(0, lambda: ...) 在主线程更新 UI

功能描述：{功能描述}
JS 文件路径：{路径}
```

### 4.2 UI 标准架构提示词

```
请按照以下标准架构生成 Python UI：

核心依赖：
- customtkinter  # UI 框架
- frida-tools    # Frida Python 绑定
- psutil         # 进程查找（可选）

标准 UI 结构：
1. 标题栏
2. 状态栏（连接状态、PID）
3. 信息面板（实时数据）
4. 操作按钮
5. 日志区域

请生成完整的 Python 代码。
```

请确保 JS 和 Python 端的交互逻辑一致。





### 4.4 自动连接机制提示词

```
请为 Python UI 添加自动连接机制：

1. 后台线程自动检测游戏进程
2. 检测到游戏进程后自动连接
3. 连接失败时重试
4. 连接成功后更新 UI 状态

游戏进程名：UnityCrossFire.exe

请生成完整的自动连接代码。
```

### 4.5 状态轮询机制提示词

```
请为 Python UI 添加状态轮询机制：

1. 启动后台线程轮询
2. 定期调用 JS 的 getstatus 函数
3. 更新 UI 显示实时数据
4. 轮询间隔 0.5 秒

请生成完整的状态轮询代码。
```

### 4.6 清理机制提示词

```
请为 Python UI 添加窗口关闭时的清理机制：

1. 停止状态轮询
2. 卸载 JS 脚本
3. 断开 Frida 连接
4. 销毁窗口

请确保资源正确释放，避免内存泄漏。

游戏进程是这个UnityCrossFire.exe

参考：
- `正式功能\10-跳过回合\AAAAA-skip_round_ui.py`
- `正式功能\09-跳高+失重\AAAAA-gravity_modifier_ui-v2.py`
- `正式功能\08-聚怪\AAAAA-bot_to_spawn_v24.py`


把需求分析发给deepseek


把js和py发给deepseek去分析，看是否有问题。
日志也发给他。
生成提示词，让他根据日志和分析结果。
不需要生成代码，只需要给我提示词，让我发给trae。


“请只针对当前报告的问题进行修复，不要重写或优化已经正常工作的其他功能代码（除非发现明确错误）。保持现有已验证模块的代码不变。”


把正式发行版，全功能整合包1.3的game_modifier.py发给他。


对话超过长度，让deepseek总结成提示词，然后另一个对话继续。
问deepseek的对话长度，是否超过了token，大概用掉了对话的百分之多少的token。
新一轮对话和旧一轮对话，互相引用，不断的了解信息。

