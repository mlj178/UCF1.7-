# 全模式房间人数：说明文字设计

## 目标

将房间人数卡片的说明改成没有标题前缀的简洁操作提示，并确保文本在卡片边框内自动换行。

## 内容与布局

- 说明文案固定为：`开启后，设置人数并点击“应用人数”。下一局生效；关闭后恢复默认人数。`
- 不显示“功能说明：”或其它标题前缀。
- 说明标签使用 250 像素换行宽度，并保留卡片左右各 10 像素的内边距。

## 验证

- 静态测试验证清单文案、面板换行宽度和无标题前缀。
- 先确认测试在旧文案上失败，再修改清单和面板默认文案。
- 完成后运行房间人数静态测试与 Python 语法检查。
