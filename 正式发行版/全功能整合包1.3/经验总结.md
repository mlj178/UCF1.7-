js和py要发给网页版deepseek检查是否有冲突。

跳过回合与无限时间在逻辑上有冲突



AAAAA-bot_to_spawn_v24.js  聚怪功能有BUG

前两次传送时，缓存里的大部分 Bot 还活着，或者说还没有积累到足够多的失效指针。第三次时累积的失效指针数量上来了，崩溃就必然发生。



快刀功能失效

你的代码只通过 `Interceptor.attach(0xAF9A90)` 被动捕获 `gm`，这要求 `AddPlayer` 函数在启用后才被调用。而实际游戏中，`AddPlayer` 通常只在玩家加入房间时触发一次，若启用时已经处于对局中，该 Hook 永远不会再执行，因此 `gm` 始终为 `null`。

**添加主动获取 `GameManager` 实例的逻辑**









