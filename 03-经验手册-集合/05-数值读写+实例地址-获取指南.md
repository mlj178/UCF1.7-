# 获取**实例地址**的方法

如何拿到ClientData的实例地址的

**1，hook**

（1）Interceptor.replace 拦截 get_isMyPlayer ，当游戏判断到"自己"时，捕获对应的 Player 对象指针。

（2）通过 Player 偏移拿到 ClientData











# 读写**字段数值**的方法

**一，hook**     （缺点就是如果一直没人引用这个方法，就无法触发hook）





​		**1，需要实例地址**

​	hook上层实例地址，通过偏移改写字段（实例指针 + 偏移量 → writeXxx() 直接修改字段值）

（1）Interceptor.replace 拦截 get_isMyPlayer ，当游戏判断到"自己"时，捕获对应的 Player 对象指针。

（2）通过 Player 偏移拿到 ClientData

（3）ClientData + 0x10 取 nickName

​		例如 recoilThis.add(0x68).writeFloat(0.0);



或者是hook ，方法，改写返回值



​	**2，不需要实例地址**

（优点，hook可以不需要实例地址，只需要方法的RVA）

（不过需要再hook中加入判断，这个player是不是玩家）

也可以不需要实例地址，直接hook   get_nickName()  方法，改写返回值







